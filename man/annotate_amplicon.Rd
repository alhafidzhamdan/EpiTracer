% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amplicon-mechanisms.R
\name{annotate_amplicon}
\alias{annotate_amplicon}
\title{Per-amplicon breakpoint annotation (shared set-up)}
\usage{
annotate_amplicon(
  this_amplicon_id,
  ecdna_gr,
  breakpoints_gr,
  cnv_gr,
  cancer_genes_gr,
  ext = 1e+07
)
}
\arguments{
\item{this_amplicon_id}{Character scalar; one value of \code{ecdna_gr$ID}.}

\item{ecdna_gr, breakpoints_gr, cnv_gr, cancer_genes_gr, ext}{See
\code{\link[=call_simple_excision]{call_simple_excision()}}.}
}
\value{
A list with \code{bp} (annotated per-breakend data.table for the amplicon),
\code{sample_bp} (all sample breakends, a GRanges), \code{cnv} (sample CN GRanges),
\code{ploidy} (numeric) and \code{amplicon_gr} (the amplicon ranges); or \code{NULL} if the
amplicon has no breakpoints in range.
}
\description{
Internal helper shared by \code{\link[=call_simple_excision]{call_simple_excision()}} and the standalone mechanism
callers (\code{\link[=call_brf]{call_brf()}}, \code{\link[=call_chimeric_amplicon]{call_chimeric_amplicon()}}, \code{\link[=call_bfb]{call_bfb()}}). For one
amplicon it builds the structural-variant breakpoint table falling within (or
just outside, by \code{ext}) the amplicon, annotated with oncogene overlap and
allele-specific copy number, and returns it together with the sample's full
breakpoint set, copy-number segments, ploidy and the amplicon ranges.
}
\keyword{internal}
