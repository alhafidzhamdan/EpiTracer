% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_chromoplexy.R
\name{call_chromoplexy}
\alias{call_chromoplexy}
\title{Call chromoplexy (closed balanced rearrangement chains)}
\usage{
call_chromoplexy(
  breakpoints_gr,
  cnv_gr,
  min_pairs = 3L,
  min_span = 1e+07,
  max_dist = 10000,
  max_cn = 3,
  footprint_width = 1e+06,
  max_small = 50000,
  min_chromosomes = 3L,
  mc.cores = 1
)
}
\arguments{
\item{breakpoints_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of SV breakends (two per
\code{event}), as used by \code{\link[=call_simple_excision]{call_simple_excision()}}: metadata \code{WGS_ID}, \code{event},
\code{svclass}, \code{PURPLE_CN}.}

\item{cnv_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of allele-specific copy-number
segments with metadata \code{sample}, \code{copyNumber}, \code{ploidy} (the per-chromosome
baseline used to ploidy-scale the low-copy filter).}

\item{min_pairs}{Integer; minimum number of deletion bridges (equivalently the
cycle length) required to call a chromoplexy (default \code{3L}, per Baca et al.).}

\item{min_span}{Numeric; minimum span (bp) for an intrachromosomal junction to
count as a long-range chain edge (default \code{1e7}); interchromosomal junctions
always qualify.}

\item{max_dist}{Integer; how close (bp) two breakends must sit to collapse into
the same locus / deletion bridge (default \code{1e4}).}

\item{max_cn}{Numeric; a junction is balanced (kept) when its copy number is
at most \code{max_cn * ploidy / 2} (default \code{3}, i.e. <=3 copies at diploid
baseline, scaled up on polysomic chromosomes).}

\item{footprint_width}{Numeric; padding (bp) applied when reporting locus
footprints (default \code{1e6}).}

\item{max_small}{Numeric; a DUP/DEL junction spanning at most this (bp) is
dropped as local noise before chain-building (default \code{5e4}).}

\item{min_chromosomes}{Integer; minimum number of DISTINCT chromosomes the
cycle must span (default \code{3L}). Chromoplexy is characteristically
interchromosomal (Baca et al.); this rejects closed rings that merely link
loci on one or two chromosomes. Set to \code{2L} (or \code{1L}) to relax.}

\item{mc.cores}{Integer; samples processed in parallel (default \code{1}).}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with one row per called chromoplasy cycle:
\code{WGS_ID}, \code{chromoplexy_id}, \code{topology} (always \code{"cycle"} for called events),
\code{n_junctions}, \code{n_bridges}, \code{n_chromosomes}, \code{chromosomes}, \code{max_cn},
\code{min_cn}, \code{min_span}, \code{n_interchrom}, \code{num_other} (intruding foreign
breakends), \code{frac_cp} (cleanliness \code{n_junctions / (n_junctions + num_other)}),
\code{footprint} and \code{events} (comma-separated \code{event} ids in the cycle, for
joining back onto amplicons). Empty (0 rows) when no chromoplexy is found.
}
\description{
Sample-level caller for chromoplexy, the punctuated formation of a closed chain
of balanced, low-copy rearrangements whose breakends meet in \emph{deletion bridges}
(Baca et al., \emph{Cell} 2013, doi:10.1016/j.cell.2013.03.021; graph criteria after
the gGnome \code{chromoplexy()} caller, Hadi et al., \emph{Cell} 2020,
doi:10.1016/j.cell.2020.08.006). Each structural variant is reconstructed as a
junction between two breakends; balanced (ploidy-scaled low-copy) junctions are
kept; breakends within \code{max_dist} collapse into loci (a locus joining two
distinct junctions is a deletion bridge); and a chromoplexy is called for every
connected component of the locus/junction graph that forms a \strong{clean simple
cycle} -- every locus of degree two, with at least \code{min_pairs} bridges closing
the ring. Runs independently of the amplicon-formation callers.
}
\seealso{
\code{\link[=call_simple_excision]{call_simple_excision()}}, \code{\link[=call_translocation_bridge_amp]{call_translocation_bridge_amp()}}
}
