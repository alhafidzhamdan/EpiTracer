% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amplicon-mechanisms.R
\name{call_chromothripsis}
\alias{call_chromothripsis}
\title{Call chromothripsis within (episomal) amplicons}
\usage{
call_chromothripsis(
  ecdna_gr = NULL,
  breakpoints_gr,
  cnv_gr,
  cancer_genes_gr,
  ext = 1e+07,
  min_cn_ratio = 3,
  seed_gap = 1e+06,
  seed_min_width = 1e+05,
  min_sv = 6L,
  min_oscillations = 3L,
  join_p = 0.05,
  mc.cores = 1
)
}
\arguments{
\item{ecdna_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of ecDNA amplicon regions with
metadata columns \code{ID} (unique amplicon identifier) and \code{WGS_ID} (sample
identifier) — typically the AmpliconArchitect amplicon catalogue. If \code{NULL}
(the default), focal-amplicon seeds are detected from \code{cnv_gr} with
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}}, so EpiTracer can run without AmpliconArchitect.}

\item{breakpoints_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline) SV
breakpoints with metadata columns \code{WGS_ID}, \code{event}, \code{svclass} (e.g. "DUP",
"DEL"), \code{PURPLE_AF}, \code{PURPLE_JCN}, \code{VF}, \code{PURPLE_CN}, \code{insLen}, \code{HOMLEN}.}

\item{cnv_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline)
allele-specific copy-number segments with metadata columns \code{sample},
\code{copyNumber}, \code{ploidy}, \code{majorAlleleCopyNumber}, \code{minorAlleleCopyNumber}.}

\item{cancer_genes_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of cancer gene loci with a
\code{gene} (gene symbol) metadata column, used to annotate breakpoints with the
overlapping oncogene.}

\item{ext}{Integer; number of base pairs to extend each amplicon by when
searching for boundary breakpoints (default \code{1e7}).}

\item{min_cn_ratio, seed_gap, seed_min_width}{Passed to
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}} when \code{ecdna_gr} is \code{NULL} (copy-number amplicon
threshold \code{copyNumber > min_cn_ratio * ploidy}, gap to merge across, and
minimum seed width). Ignored when \code{ecdna_gr} is supplied.}

\item{min_sv}{Minimum number of distinct internal SV events for the prevalence
hallmark (default 6).}

\item{min_oscillations}{Minimum copy-number direction changes (turning points)
across the footprint segments for the oscillation hallmark (default 3).}

\item{join_p}{Significance threshold for the fragment-join randomness test; the
junction-orientation distribution must NOT differ from uniform at this level
(default 0.05).}

\item{mc.cores}{Integer; number of cores for \code{\link[parallel:mclapply]{parallel::mclapply()}}
(default \code{1}). Values > 1 are ignored on Windows.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} of annotated breakpoints with \code{chromothripsis}
(\code{"TRUE"}/\code{"FALSE"}), \code{chromothripsis_conf} (\code{"high"}/\code{"low"}/\code{"none"}),
\code{n_internal_sv}, \code{n_intrachr_sv}, \code{sv_type_pval}, \code{cn_oscillations} and
\code{loh_interspersed}.
}
\description{
Standalone caller that scores each amplicon's INTERNAL structural variants for
chromothripsis, using the ShatterSeek hallmarks (Cortes-Ciriano et al., Nat
Genet 2020) restricted to the amplified footprint. It is designed to be run on
amplicons already called episomal (pass them as \code{ecdna_gr}) to flag the subset
that have since shattered -- the ecDNA -> micronucleus -> chromothripsis route
-- but works on any amplicon catalogue (or \code{NULL} to auto-detect seeds).
}
\details{
A footprint is called chromothriptic when it carries at least \code{min_sv} distinct
internal SV events, its four intrachromosomal junction orientations are close to
equally represented (chi-squared goodness-of-fit p >= \code{join_p}, i.e. random
fragment joins), and its rounded copy-number profile changes direction at least
\code{min_oscillations} times across the footprint (oscillating copy number). All
three give \code{chromothripsis_conf = "high"}. Random fragment joins are required
for any positive call (they separate chromothripsis from orientation-biased
mechanisms such as BFB); prevalence with random joins but weak oscillation gives
\code{"low"}. A clean simple episome (one boundary junction, few internal SVs) fails
the prevalence test and is called \code{"FALSE"}.
}
\seealso{
\code{\link[=call_simple_excision]{call_simple_excision()}}, \code{\link[=call_chimeric_amplicon]{call_chimeric_amplicon()}}, \code{\link[=call_bfb]{call_bfb()}}
}
