% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_episomal_ecdna.R
\name{call_episomal_ecdna}
\alias{call_episomal_ecdna}
\title{Call episomal extrachromosomal DNA from WGS structural variants}
\usage{
call_episomal_ecdna(
  ecdna_gr = NULL,
  breakpoints_gr,
  cnv_gr,
  cancer_genes_gr,
  ext = 1e+07,
  mc.cores = 1L,
  verbose = FALSE,
  min_cn_ratio = 3,
  seed_gap = 1e+06,
  seed_min_width = 1e+05
)
}
\arguments{
\item{ecdna_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of ecDNA amplicon regions with
metadata columns \code{ID} (unique amplicon identifier) and \code{WGS_ID} (sample
identifier) — typically the AmpliconArchitect amplicon catalogue. If \code{NULL}
(the default), focal-amplicon seeds are detected from \code{cnv_gr} with
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}}, so EpiTracer can run without AmpliconArchitect.}

\item{breakpoints_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline) SV
breakpoints with metadata columns \code{WGS_ID}, \code{event}, \code{svclass} (e.g. "DUP",
"DEL"), \code{PURPLE_AF}, \code{PURPLE_JCN}, \code{VF}, \code{PURPLE_CN}, \code{insLen}, \code{HOMLEN}.}

\item{cnv_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline)
allele-specific copy-number segments with metadata columns \code{sample},
\code{copyNumber}, \code{ploidy}, \code{majorAlleleCopyNumber}, \code{minorAlleleCopyNumber}.}

\item{cancer_genes_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of cancer gene loci with a
\code{gene} (gene symbol) metadata column, used to annotate breakpoints with the
overlapping oncogene.}

\item{ext}{Integer; number of base pairs to extend each amplicon by when
searching for boundary breakpoints (default \code{1e7}).}

\item{mc.cores}{Integer; number of cores for \code{\link[parallel:mclapply]{parallel::mclapply()}}
(default \code{1}). Values > 1 are ignored on Windows.}

\item{verbose}{Logical; print per-amplicon progress (default \code{FALSE}).}

\item{min_cn_ratio, seed_gap, seed_min_width}{Passed to
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}} when \code{ecdna_gr} is \code{NULL} (copy-number amplicon
threshold \code{copyNumber > min_cn_ratio * ploidy}, gap to merge across, and
minimum seed width). Ignored when \code{ecdna_gr} is supplied.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} combining the annotated breakpoints of all
amplicons, with per-breakpoint classification columns including
\code{duplication_at_boundary}, \code{duplication_at_boundary_has_highest_VF},
\code{episome_region}, \code{deletion_flanking_boundary}, \code{episomal}, and
\code{has_excision_scar} (all character \code{"TRUE"}/\code{"FALSE"}).
}
\description{
Detects ecDNA amplicons whose structure is consistent with the \emph{episome}
(breakage-independent) model of formation: a circular amplicon bounded by a
duplication breakpoint, arising from an otherwise non-amplified chromosomal
region, and often leaving a deletion "excision scar" at the origin locus.
}
\details{
Each amplicon called by AmpliconArchitect (supplied in \code{ecdna_gr}, a
compulsory input) is processed independently (optionally in parallel). Within
each amplicon the function annotates the flanking structural variant
breakpoints with oncogene and allele-specific copy-number context, then
applies the heuristic described in \code{\link[=classify_amplicon_episomal]{classify_amplicon_episomal()}}.
}
\examples{
\dontrun{
ecdna_gr        <- readRDS("ecDNA_amplicon_regions.rds")
breakpoints_gr  <- readRDS("SV_catalogue.rds")
cnv_gr          <- readRDS("CN_segments.rds")
cancer_genes_gr <- readRDS("cancer_genes.rds")

episomal <- call_episomal_ecdna(
  ecdna_gr, breakpoints_gr, cnv_gr, cancer_genes_gr,
  ext = 1e7, mc.cores = 4
)
}

}
\seealso{
\code{\link[=classify_amplicon_episomal]{classify_amplicon_episomal()}}, \code{\link[=plot_sv_linear]{plot_sv_linear()}}
}
