% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/founder_boundary.R
\name{call_founder_boundary}
\alias{call_founder_boundary}
\title{Nominate episomes by a VF-early circularisation DUP}
\usage{
call_founder_boundary(
  ecdna_gr = NULL,
  breakpoints_gr,
  cnv_gr,
  cancer_genes_gr,
  ext = 1e+07,
  min_cn_ratio = 3,
  seed_gap = 1e+06,
  seed_min_width = 5000,
  max_k = 4L,
  span_frac = 0.8,
  early_strata = 2L,
  mc.cores = 1
)
}
\arguments{
\item{ecdna_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of ecDNA amplicon regions with
metadata columns \code{ID} (unique amplicon identifier) and \code{WGS_ID} (sample
identifier) — typically the AmpliconArchitect amplicon catalogue. If \code{NULL}
(the default), focal-amplicon seeds are detected from \code{cnv_gr} with
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}}, so EpiTracer can run without AmpliconArchitect.}

\item{breakpoints_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline) SV
breakpoints with metadata columns \code{WGS_ID}, \code{event}, \code{svclass} (e.g. "DUP",
"DEL"), \code{PURPLE_AF}, \code{PURPLE_JCN}, \code{VF}, \code{PURPLE_CN}, \code{insLen}, \code{HOMLEN}.}

\item{cnv_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline)
allele-specific copy-number segments with metadata columns \code{sample},
\code{copyNumber}, \code{ploidy}, \code{majorAlleleCopyNumber}, \code{minorAlleleCopyNumber}.}

\item{cancer_genes_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of cancer gene loci with a
\code{gene} (gene symbol) metadata column, used to annotate breakpoints with the
overlapping oncogene.}

\item{ext}{Integer; number of base pairs to extend each amplicon by when
searching for boundary breakpoints (default \code{1e7}).}

\item{min_cn_ratio, seed_gap, seed_min_width}{Passed to
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}} when \code{ecdna_gr} is \code{NULL} (copy-number amplicon
threshold \code{copyNumber > min_cn_ratio * ploidy}, gap to merge across, and
minimum seed width). Ignored when \code{ecdna_gr} is supplied.}

\item{max_k}{Maximum number of VF strata (default 4), as in
\code{\link[=plot_sv_reconstruction]{plot_sv_reconstruction()}}.}

\item{span_frac}{A boundary DUP must span at least this fraction of the
amplicon footprint to count (default 0.8).}

\item{early_strata}{Highest stratum index still considered "early" (default 2:
the founder and the top cluster).}

\item{mc.cores}{Integer; number of cores for \code{\link[parallel:mclapply]{parallel::mclapply()}}
(default \code{1}). Values > 1 are ignored on Windows.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} of annotated breakpoints with
\code{founder_class} (svclass of the max-VF copy-gaining junction: DUP or an
inversion), \code{founder_vf}, \code{early_boundary_dup} (\code{"TRUE"}/\code{"FALSE"}),
\code{early_dup_vf}, \code{early_dup_stratum}, \code{n_strata}, \code{interlocus_tra}
(\code{"TRUE"}/\code{"FALSE"}; a translocation to another locus), \code{max_tra_vf} and
\code{n_ancestral_del} (deletions excluded from the founder call).
}
\description{
Standalone caller that VF-stratifies each amplicon's junctions (as
\code{\link[=plot_sv_reconstruction]{plot_sv_reconstruction()}} does) and flags a footprint-spanning boundary DUP in
the EARLY tier -- the founder or the highest-VF cluster ("cluster 1"). The
circularisation DUP need not be the single highest-VF junction; an inverted-
duplication amplicon's fold-back can carry more copies, so requiring the boundary
DUP merely to be \emph{early} rather than \emph{maximal} is the correct test.
}
\details{
The founding event of an amplicon must be a copy-GAINING junction: a DUP
(circularisation / tandem) or an inversion (inverted duplication). A DELETION
removes sequence and therefore cannot found an amplification -- a high-VF
deletion is an ANCESTRAL event (high-copy only because every copy carries it),
not a founder -- so deletions are excluded from the founding-mechanism call and
counted in \code{n_ancestral_del}. \code{founder_class} is thus DUP (simple excision) or
an inversion (inverted duplication, not simple excision).

Each amplicon is assessed on its OWN (intrachromosomal) junctions:
inter-locus translocations are excluded from the founding-mechanism call and
reported separately (\code{interlocus_tra}, \code{max_tra_vf}), because a TRA joins this
circle to another amplicon rather than forming it. Two independently-episomal
(DUP-founder) loci linked by a TRA are the two-ecDNA-recombination (micronuclear
chromothripsis) hypothesis.
}
\seealso{
\code{\link[=plot_sv_reconstruction]{plot_sv_reconstruction()}}, \code{\link[=call_simple_excision]{call_simple_excision()}}
}
