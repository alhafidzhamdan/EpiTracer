% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_simple_excision.R
\name{call_simple_excision}
\alias{call_simple_excision}
\title{Call episomal extrachromosomal DNA from WGS structural variants}
\usage{
call_simple_excision(
  ecdna_gr = NULL,
  breakpoints_gr,
  cnv_gr,
  cancer_genes_gr,
  ext = 1e+07,
  mc.cores = 1L,
  verbose = FALSE,
  min_cn_ratio = 3,
  seed_gap = 1e+06,
  seed_min_width = 5000,
  flank_baseline = c("chromosome", "ploidy"),
  gain_ratio = 1.4,
  min_flank_width = 2000,
  bridge_gap = seed_gap,
  founder_jcn = 30,
  centromeres = NULL,
  mh_min_homology = 2,
  hr_min_homology = 14
)
}
\arguments{
\item{ecdna_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of ecDNA amplicon regions with
metadata columns \code{ID} (unique amplicon identifier) and \code{WGS_ID} (sample
identifier) — typically the AmpliconArchitect amplicon catalogue. If \code{NULL}
(the default), focal-amplicon seeds are detected from \code{cnv_gr} with
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}}, so EpiTracer can run without AmpliconArchitect.}

\item{breakpoints_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline) SV
breakpoints with metadata columns \code{WGS_ID}, \code{event}, \code{svclass} (e.g. "DUP",
"DEL"), \code{PURPLE_AF}, \code{PURPLE_JCN}, \code{VF}, \code{PURPLE_CN}, \code{insLen}, \code{HOMLEN}.}

\item{cnv_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline)
allele-specific copy-number segments with metadata columns \code{sample},
\code{copyNumber}, \code{ploidy}, \code{majorAlleleCopyNumber}, \code{minorAlleleCopyNumber}.}

\item{cancer_genes_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of cancer gene loci with a
\code{gene} (gene symbol) metadata column, used to annotate breakpoints with the
overlapping oncogene.}

\item{ext}{Integer; number of base pairs to extend each amplicon by when
searching for boundary breakpoints (default \code{1e7}).}

\item{mc.cores}{Integer; number of cores for \code{\link[parallel:mclapply]{parallel::mclapply()}}
(default \code{1}). Values > 1 are ignored on Windows.}

\item{verbose}{Logical; print per-amplicon progress (default \code{FALSE}).}

\item{min_cn_ratio, seed_gap, seed_min_width}{Passed to
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}} when \code{ecdna_gr} is \code{NULL} (copy-number amplicon
threshold \code{copyNumber > min_cn_ratio * ploidy}, gap to merge across, and
minimum seed width). Ignored when \code{ecdna_gr} is supplied.}

\item{flank_baseline}{How the "flanks not gained" test is calibrated:
\code{"chromosome"} (default) compares the amplicon flanks to the local
per-chromosome baseline (the width-weighted median copy number of that
chromosome's non-focally-amplified segments), \code{"ploidy"} compares them to
the global sample ploidy. Use \code{"chromosome"} for focal episomes on a
polysomic chromosome (e.g. EGFR on the gained chr7 in glioblastoma), which
\code{"ploidy"} misses because the flanks sit above tumour ploidy.}

\item{gain_ratio}{Numeric; a flank is "gained" when its copy number is at or
above \code{gain_ratio} times the baseline (default \code{1.4}).}

\item{min_flank_width}{Integer; ignore copy-number segments narrower than this
(in bp) when reading the flanking copy number, so a sub-kb "segmentation
shoulder" emitted at a sharp amplicon edge is not mistaken for the true
flank (default \code{2000}). The nearest segment at least this wide is tested;
if none qualifies, the abutting segment is used as a fallback.}

\item{bridge_gap}{Numeric; the maximum non-amplified gap (in bp) an
amplicon-connecting junction may span before the amplicon is flagged as
\code{bridging} and rejected (default: \code{seed_gap}, i.e. the same distance that
separates distinct amplicon seeds). A junction whose both breakends sit at
amplicon-level copy number but that spans a wider gap fuses two separate
amplicons rather than bounding one episome; a genuine internal deletion
leaves only a sub-\code{bridge_gap} gap.}

\item{founder_jcn}{Numeric; the junction-copy-number above which an SV counts
as a clonal, high-copy "founder" junction for the micronucleus test (default
\code{30}). When a founder-level inversion begins inside a founder-level
duplication's span (interleaved DUP+INV), the amplicon is a micronucleus /
chromothripsis product and is called non-episomal. Low-JCN (subclonal)
interleaving -- later internal rearrangement of a genuine episome -- is
ignored.}

\item{centromeres}{Optional \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of centromere spans (one
per chromosome, e.g. from \code{\link[=load_centromeres]{load_centromeres()}}). When supplied, an amplicon
whose amplified edge terminates \emph{within} a centromere (rather than
traversing across it) is flagged \code{flag_chromosomal_bridge} and excluded from
the episomal call as a dicentric / chromosomal-bridge event. \code{NULL} (default)
skips the check.}

\item{mh_min_homology, hr_min_homology}{Numeric; breakpoint-homology (bp)
thresholds used to classify the circularisation (boundary-DUP) junction into
an inferred DSB-repair pathway, following Eugen-Olsen et al. (Nucleic Acids
Res 2025; doi:10.1093/nar/gkaf122). A junction with homology
\verb{< mh_min_homology} is called \code{"NHEJ"} (near-blunt), \verb{>= mh_min_homology} and
\verb{< hr_min_homology} is \code{"MMEJ"} (short microhomology / alternative
end-joining), and \verb{>= hr_min_homology} is \code{"HR"} (long homology / homologous
recombination). Defaults \code{2} and \code{14} are the human microhomology and HR
minimum-homology lengths reported in that review.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} combining the annotated breakpoints of all
amplicons, with per-breakpoint classification columns including
\code{duplication_at_boundary}, \code{duplication_at_boundary_has_highest_VF},
\code{episome_region}, \code{deletion_flanking_boundary}, \code{episomal},
\code{has_excision_scar}, and the annotation-only flags \code{flag_internal_sv_high_vf}
(an internal SV out-VFs the boundary DUP), \code{flag_internal_inversion} (a
fold-back inversion does so), \code{flag_micronucleus} (interleaved high-JCN
DUP+INV) and \code{flag_bridging_amplicon} (a junction fuses two separate
amplicons) -- all character \code{"TRUE"}/\code{"FALSE"} and none of which override the
call. The only disqualifying flags are \code{flag_inv_at_boundary} /
\code{flag_tra_at_boundary} (an edge is defined by an INV/TRA, not a
circularisation) and \code{flag_chromosomal_bridge} (an edge terminates in a
centromere); an amplicon carrying any of these is set \code{episomal = "FALSE"}.
For an amplicon with a
boundary DUP it also reports the circularisation-junction microhomology as
\code{boundary_homology} (numeric bp) and \code{junction_homology_class}
(\code{"NHEJ"}/\code{"MMEJ"}/\code{"HR"}; \code{NA} when no boundary DUP is found), the inferred
DSB-repair pathway that sealed the circle.

The other amplicon-formation mechanisms are computed by dedicated standalone
callers, not here: \code{\link[=call_brf]{call_brf()}} (breakage-replication/fusion / adjacent
parallel breakpoints), \code{\link[=call_chimeric_amplicon]{call_chimeric_amplicon()}} (high-VF non-homologous
translocation), \code{\link[=call_bfb]{call_bfb()}} (breakage-fusion-bridge) and
\code{\link[=call_translocation_bridge_amp]{call_translocation_bridge_amp()}} (translocation-bridge amplification). Join
their output to this one by \code{WGS_ID} + \code{ID} to assemble a combined mechanism
table.
}
\description{
Detects ecDNA amplicons whose structure is consistent with the \emph{episome}
(breakage-independent) model of formation: a circular amplicon bounded by a
duplication breakpoint, arising from an otherwise non-amplified chromosomal
region, and often leaving a deletion "excision scar" at the origin locus.
}
\details{
Each amplicon called by AmpliconArchitect (supplied in \code{ecdna_gr}, a
compulsory input) is processed independently (optionally in parallel). Within
each amplicon the function annotates the flanking structural variant
breakpoints with oncogene and allele-specific copy-number context, then
applies the heuristic described in \code{\link[=classify_amplicon_episomal]{classify_amplicon_episomal()}}.
}
\examples{
\dontrun{
ecdna_gr        <- readRDS("ecDNA_amplicon_regions.rds")
breakpoints_gr  <- readRDS("SV_catalogue.rds")
cnv_gr          <- readRDS("CN_segments.rds")
cancer_genes_gr <- readRDS("cancer_genes.rds")

episomal <- call_simple_excision(
  ecdna_gr, breakpoints_gr, cnv_gr, cancer_genes_gr,
  ext = 1e7, mc.cores = 4
)
}

}
\seealso{
\code{\link[=classify_amplicon_episomal]{classify_amplicon_episomal()}}, \code{\link[=plot_sv_linear]{plot_sv_linear()}}
}
