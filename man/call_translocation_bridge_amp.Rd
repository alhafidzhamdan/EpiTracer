% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amplicon-mechanisms.R
\name{call_translocation_bridge_amp}
\alias{call_translocation_bridge_amp}
\title{Call translocation-bridge amplification (TBA) amplicons}
\usage{
call_translocation_bridge_amp(
  ecdna_gr = NULL,
  breakpoints_gr,
  cnv_gr,
  cancer_genes_gr,
  ext = 1e+07,
  min_cn_ratio = 3,
  seed_gap = 1e+06,
  seed_min_width = 1e+05,
  tb_edge_tol = 10000,
  centromeres = NULL,
  chrom_lengths = NULL,
  loh_max = 0.5,
  bridge_loh_min_frac = 0.15,
  nonbridge_loh_max_frac = 0.1,
  bridge_anchor_tol = 3e+06,
  mc.cores = 1
)
}
\arguments{
\item{ecdna_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of ecDNA amplicon regions with
metadata columns \code{ID} (unique amplicon identifier) and \code{WGS_ID} (sample
identifier) — typically the AmpliconArchitect amplicon catalogue. If \code{NULL}
(the default), focal-amplicon seeds are detected from \code{cnv_gr} with
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}}, so EpiTracer can run without AmpliconArchitect.}

\item{breakpoints_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline) SV
breakpoints with metadata columns \code{WGS_ID}, \code{event}, \code{svclass} (e.g. "DUP",
"DEL"), \code{PURPLE_AF}, \code{PURPLE_JCN}, \code{VF}, \code{PURPLE_CN}, \code{insLen}, \code{HOMLEN}.}

\item{cnv_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of PURPLE (HMF pipeline)
allele-specific copy-number segments with metadata columns \code{sample},
\code{copyNumber}, \code{ploidy}, \code{majorAlleleCopyNumber}, \code{minorAlleleCopyNumber}.}

\item{cancer_genes_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of cancer gene loci with a
\code{gene} (gene symbol) metadata column, used to annotate breakpoints with the
overlapping oncogene.}

\item{ext}{Integer; number of base pairs to extend each amplicon by when
searching for boundary breakpoints (default \code{1e7}).}

\item{min_cn_ratio, seed_gap, seed_min_width}{Passed to
\code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}} when \code{ecdna_gr} is \code{NULL} (copy-number amplicon
threshold \code{copyNumber > min_cn_ratio * ploidy}, gap to merge across, and
minimum seed width). Ignored when \code{ecdna_gr} is supplied.}

\item{tb_edge_tol}{Integer; how close (bp) a translocation breakend must sit to
an amplified amplicon edge to count as a boundary translocation (default
\code{1e4}).}

\item{centromeres, chrom_lengths}{Optional \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of
centromere spans (e.g. \code{\link[=load_centromeres]{load_centromeres()}}) and named chromosome-length
vector (\code{\link[=load_chrom_lengths]{load_chrom_lengths()}}). When both are supplied, each TBA amplicon is
additionally scored for the paper's confirmatory footprint (see \code{tb_confident}
in the return value); without them the confidence columns are \code{"NA"}.}

\item{loh_max}{Numeric; a copy-number segment is loss-of-heterozygosity (LOH)
when its minor-allele copy number is below this (default \code{0.5}, i.e. ~0).}

\item{bridge_loh_min_frac}{Numeric; minimum fraction of a "bridge" arm that
must be LOH, with an LOH block anchored near a landmark (telomere OR
centromere), for that arm to count as a bridge arm (default \code{0.15}).}

\item{nonbridge_loh_max_frac}{Numeric; maximum LOH fraction allowed on the
amplicon chromosome's opposite ("non-bridge") arm for it to count as spared
(default \code{0.1}).}

\item{bridge_anchor_tol}{Integer; how close (bp) an LOH block must reach to a
telomere or centromere to anchor the bridge-arm LOH (default \code{3e6}). The
tolerance lets centromere-proximal LOH that starts a megabase or two past the
centromere (a heterozygous peri-centromeric sliver in between) still anchor.}

\item{mc.cores}{Integer; number of cores for \code{\link[parallel:mclapply]{parallel::mclapply()}}
(default \code{1}). Values > 1 are ignored on Windows.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} of annotated breakpoints with \code{tba}
(\code{"TRUE"}/\code{"FALSE"}), \code{n_boundary_tra} (number of distinct boundary
translocations) and \code{tb_partner_chr} (comma-separated partner chromosome(s)).
When \code{centromeres} and \code{chrom_lengths} are supplied it also reports the
confirmatory footprint of Lee et al. (\emph{Nature} 2023): \code{tb_bridge_arm_loh}
(bridge-arm LOH on the amplicon's own arm, to a telomere or centromere),
\code{tb_partner_arm_loh} (the same on a translocated partner arm),
\code{tb_nonbridge_spared} (the amplicon chromosome's opposite arm retains
heterozygosity), \code{tb_confident} (\code{"TRUE"} when a bridge arm -- amplicon
and/or partner -- shows LOH AND the non-bridge arm is spared: the asymmetric
pattern distinguishing TB amplification from symmetric chromothripsis) and
\code{tb_high_confidence} (\code{"TRUE"} only for the dual-LOH pattern: LOH on BOTH the
amplicon and partner bridge arms with the non-bridge arm spared -- the
strongest signature of a dicentric translocation bridge).
}
\description{
Standalone caller for translocation-bridge (TB) amplification (Lee, Kim,
..., Park, \emph{Nature} 2023; doi:10.1038/s41586-023-06057-w): a focal amplicon
whose boundary is defined by an \strong{inter-chromosomal translocation that is
itself amplified} (the "boundary translocation" carried within the amplicon).
In this model an oncogene neighbourhood is translocated in G1, creating a
dicentric chromosome that forms a chromosome bridge in mitosis; its breakage
and repair amplify the fragment (often as ecDNA), leaving an amplified
translocation demarcating the amplicon edge and co-amplifications on the
partner chromosome. Runs independently of the other mechanism callers.
}
\seealso{
\code{\link[=call_simple_excision]{call_simple_excision()}}, \code{\link[=call_bfb]{call_bfb()}}, \code{\link[=call_chimeric_amplicon]{call_chimeric_amplicon()}}
}
