% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_wgd.R
\name{call_wgd}
\alias{call_wgd}
\title{Call whole-genome doubling (WGD) from allele-specific copy number}
\usage{
call_wgd(cnv_gr)
}
\arguments{
\item{cnv_gr}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges}, \code{data.frame} or
\link[data.table:data.table]{data.table::data.table} of copy-number segments carrying
\code{majorAlleleCopyNumber} (and \code{seqnames}/\code{start}/\code{end}, taken from the ranges
for a GRanges). An optional \code{sample} column splits multi-sample input; when
absent the whole table is treated as one sample. Chromosome names may be with
or without the \code{chr} prefix.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with one row per \code{sample}:
\code{n_wgd_autosomes} (how many autosomes meet the >1.5-over-half-bases test) and
\code{wgd} (logical; \code{n_wgd_autosomes >= 11}).
}
\description{
Applies PURPLE's whole-genome-doubling rule: a sample is WGD when its
\strong{major-allele copy number exceeds 1.5 across at least half of the bases} on
\strong{at least 11 of the 22 autosomes}. Operates directly on the allele-specific
copy-number segments EpiTracer already uses, so WGD status needs no separate
input file.
}
\seealso{
\code{\link[=plot_sv_circos]{plot_sv_circos()}}
}
