% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-amplicon.R
\name{ecdna_sim}
\alias{ecdna_sim}
\title{Simulated ecDNA amplicon}
\description{
The object returned by \code{\link[=sim_episome]{sim_episome()}} and transformed by the mechanism
operators (\code{\link[=sim_micronucleation]{sim_micronucleation()}}, \code{\link[=sim_fuse_episomes]{sim_fuse_episomes()}}, \code{\link[=sim_evolve]{sim_evolve()}}).
It records the \emph{ground truth} structure of one simulated amplicon, from which
\code{\link[=sim_to_epitracer]{sim_to_epitracer()}} renders caller inputs and \code{\link[=sim_to_plot_inputs]{sim_to_plot_inputs()}} renders
plotter inputs.
}
\section{Components}{

\describe{
\item{\code{sample}}{Character sample identifier.}
\item{\code{fragments}}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} in \strong{amplicon order} -- the
sequence of intervals a polymerase would traverse walking the circle once.
Metadata \code{inv} (\code{0} forward, \code{1} inverted) and \code{origin} (the label of the
parental episome a fragment came from, which distinguishes the two halves
of a chimera).}
\item{\code{circular}}{\code{TRUE} for an episome; the last fragment joins the first.}
\item{\code{copies}}{Numeric ecDNA copies per tumour cell. Copy number is
\code{copies * coverage(fragments)} on top of the host baseline, so this sets
the amplicon's copy-number level while \code{fragments} sets its shape.}
\item{\code{junctions}}{A \link[data.table:data.table]{data.table::data.table} ledger of every junction with
\code{chrom1}, \code{start1}, \code{strand1}, \code{chrom2}, \code{start2}, \code{strand2}, \code{svclass},
\code{multiplicity} (traversals per circle), \code{round}, \code{mechanism}, \code{origin}
(\code{"amplicon"} or \code{"host"}) and \code{homology} (the repair pathway).}
\item{\code{host}}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of host-chromosome background with
\code{ploidy}, giving the local copy-number baseline the flanks sit at.}
\item{\code{variants}}{\code{NULL} for a homogeneous amplicon. Once a single molecule
is altered (see \code{\link[=sim_internal_deletion]{sim_internal_deletion()}}) the cell carries a MIXTURE, and
this holds it as a list of \code{list(fragments, copies, label)}. \code{fragments}
and \code{copies} above then report the most abundant species and the total.
The mixture is what makes the ORDER of events observable: a junction that
arose late is carried by fewer circles, so it is emitted at lower copy
number and lower read support. Each species also carries a \code{fitness}, its
replication rate relative to the founder, which is what lets a minority
species expand rather than sit at a frozen share.}
\item{\code{preserved}}{Oncogene loci that mechanisms must not delete.}
\item{\code{history}}{Character log of the mechanism steps applied, in order.}
\item{\code{round}}{Integer count of evolutionary rounds applied so far.}
}
}

\seealso{
\code{\link[=sim_episome]{sim_episome()}}, \code{\link[=sim_micronucleation]{sim_micronucleation()}}, \code{\link[=sim_to_epitracer]{sim_to_epitracer()}}
}
