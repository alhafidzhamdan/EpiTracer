% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{ex_recon_inputs}
\alias{ex_recon_inputs}
\title{Example inputs for \code{plot_sv_reconstruction()}: a complex CDK4 amplicon}
\format{
A named \code{list} of three \code{data.frame}s -- \code{cnv_data}, \code{sv_data},
\code{wgd_data} -- matching the columns \code{\link[=plot_sv_reconstruction]{plot_sv_reconstruction()}} expects
(chr12, ~57.4-59.6 Mb around CDK4).
}
\source{
Synthetic; generated by \code{data-raw/make_example_data.R} (statistics
modelled on a real complex CDK4 amplicon, but all coordinates simulated).
}
\usage{
ex_recon_inputs
}
\description{
A synthetic but realistic complex focal amplification -- a heavily rearranged
\strong{CDK4} amplicon on chr12 (sample \code{"EXAMPLE02"}) whose statistics are
modelled on a real ecDNA amplicon: ~250 intra-chromosomal junctions
(DEL/DUP/inversions) spanning read support from \code{VF} ~4 up to a \code{VF}-496
founder, over an oscillating copy-number profile (CN 15-35). So
\code{\link[=plot_sv_reconstruction]{plot_sv_reconstruction()}} rebuilds it wave by wave from the founder down.
\code{karyotype} / \code{gene_coord} fall back to the bundled hg38 references (CDK4 is
labelled).
}
\examples{
p <- plot_sv_reconstruction(
  sample   = "EXAMPLE02",
  cnv_data = ex_recon_inputs$cnv_data,
  sv_data  = ex_recon_inputs$sv_data,
  wgd_data = ex_recon_inputs$wgd_data,
  chromosome = "chr12", chromosome_range = matrix(c(57e6, 60e6), nrow = 1),
  min_vf = 10
)
}
\seealso{
\code{\link[=plot_sv_reconstruction]{plot_sv_reconstruction()}}, \link{ex_plot_inputs}
}
\keyword{datasets}
