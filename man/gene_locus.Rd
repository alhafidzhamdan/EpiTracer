% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-inputs.R
\name{gene_locus}
\alias{gene_locus}
\title{Resolve gene symbol(s) to a plotting locus}
\usage{
gene_locus(
  genes,
  genome = "hg38",
  cnv = NULL,
  sample = NULL,
  flank = 5e+05,
  target = c("any", "amp", "homdel"),
  min_cn_ratio = 3,
  homdel_thresh = 0.5
)
}
\arguments{
\item{genes}{Character vector of gene symbols (case-insensitive).}

\item{genome}{One of \code{"hg38"}, \code{"hg19"}, \code{"mm10"}.}

\item{cnv}{Optional copy-number table (from \code{\link[=read_cnv]{read_cnv()}}) to size the window to
the overlapping event.}

\item{sample}{Optional sample to subset \code{cnv} by.}

\item{flank}{Bp added on each side of the resolved region (default \code{5e5}).}

\item{target}{\code{"amp"} sizes to an amplified segment, \code{"homdel"} to a homozygous
deletion, \code{"any"} (default) to the gene span.}

\item{min_cn_ratio, homdel_thresh}{Amplification (\code{copyNumber > min_cn_ratio * ploidy}) and homozygous-deletion (\code{copyNumber < homdel_thresh}) thresholds.}
}
\value{
A data.frame with \code{chr}, \code{start}, \code{end}, \code{gene} (one row per found gene).
}
\description{
Looks a gene symbol up in the bundled oncogene panel for \code{genome} and returns a
window suitable for \code{\link[=plot_sv_linear]{plot_sv_linear()}}'s \code{loci} argument, so a user can ask to
plot e.g. \code{"EGFR"} or \code{"CDKN2A"} by name. When \code{cnv} is supplied the window is
grown to the copy-number event (amplification or homozygous deletion) overlapping
the gene, so the zoom fits the event; otherwise the gene span plus \code{flank} is used.
}
\seealso{
\code{\link[=plot_sv_linear]{plot_sv_linear()}}, \code{\link[=read_cnv]{read_cnv()}}
}
