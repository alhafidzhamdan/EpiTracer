% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_amplicon_recon.R
\name{plot_amplicon_recon}
\alias{plot_amplicon_recon}
\title{Multi-locus amplicon "recon" plot on a single concatenated axis}
\usage{
plot_amplicon_recon(
  sample,
  cnv_data,
  sv_data,
  wgd_data,
  karyotype,
  gene_coord,
  loci = NULL,
  cds_gr = NULL,
  margin = 0.15,
  min_cn_ratio = 3,
  min_amp_width = 1e+05,
  gap_frac = 0.06,
  genes_to_highlight = NULL,
  gene_label_angle = NULL,
  repel_labels = TRUE,
  offset_gene = 1.15,
  ymax_highlight_ratio = 1.08,
  karyotype_rel_size = 0.06,
  loh_position_ratio = 0.5,
  highlight_amp = TRUE,
  highlight_hom_del = TRUE,
  sec_axis_adj = 10,
  wgd_sample_col = NULL,
  outdir = NULL,
  save = TRUE,
  format = c("pdf", "png"),
  dpi = 300,
  plot_width_custom = NULL,
  plot_height_custom = NULL,
  verbose = FALSE
)
}
\arguments{
\item{sample}{Character scalar; sample identifier (matched in \code{cnv_data},
\code{sv_data}, \code{wgd_data}).}

\item{cnv_data, sv_data, wgd_data, karyotype, gene_coord, cds_gr}{As in
\code{\link[=plot_sv_linear]{plot_sv_linear()}}.}

\item{loci}{Optional description of the loci to show. Either a \code{data.frame}
with columns \code{chr},\code{start},\code{end}, or a character vector of \code{"chr:start-end"}
strings. If \code{NULL} (default), amplified loci are detected automatically.}

\item{margin}{Numeric; fraction of each amplicon's width to pad on both sides
when focusing (default \code{0.15}, i.e. +/-15\%).}

\item{min_cn_ratio}{Numeric; a segment is "amplified" when
\code{copyNumber > min_cn_ratio * ploidy} (default \code{3}).}

\item{min_amp_width}{Numeric; drop auto-detected loci whose total amplified
span is below this many bp (default \code{1e5}), removing single-segment
artefacts.}

\item{gap_frac}{Numeric; width of the gap between loci as a fraction of the
total plotted width (default \code{0.06}).}

\item{genes_to_highlight, gene_label_angle, repel_labels}{Gene-label control, as
in \code{\link[=plot_sv_linear]{plot_sv_linear()}}.}

\item{offset_gene, ymax_highlight_ratio, karyotype_rel_size, loh_position_ratio}{Layout tuning, as in \code{\link[=plot_sv_linear]{plot_sv_linear()}}.}

\item{highlight_amp, highlight_hom_del}{Logical; shade amplified / homozygously
deleted segments.}

\item{sec_axis_adj}{Retained for compatibility; unused.}

\item{outdir, save, format, dpi, plot_width_custom, plot_height_custom, verbose}{Output control, as in \code{\link[=plot_sv_linear]{plot_sv_linear()}}.}
}
\value{
The assembled \link[ggplot2:ggplot]{ggplot2::ggplot} object (invisibly when a file is
written); written paths are attached as attribute \code{"path"}.
}
\description{
Draws allele-specific copy number, structural-variant arcs, karyotype
ideograms and gene labels for several amplified loci placed side-by-side on a
single concatenated x-axis. Because all loci share one coordinate system, the
structural variants that interconnect separate amplicons (e.g. the junctions
of a multi-fragment / hub ecDNA) are drawn as arcs spanning the loci, rather
than stretching individual panels as they would under faceting
(contrast \code{\link[=plot_sv_linear]{plot_sv_linear()}}, which is best for one locus or whole
chromosomes).
}
\details{
Each locus is focused on its amplified region: pass explicit windows via
\code{loci}, or leave \code{loci = NULL} to auto-detect the amplified span on each
chromosome (\code{copyNumber > min_cn_ratio * ploidy}) and pad it by \code{margin}.
}
\examples{
\dontrun{
plot_amplicon_recon(
  sample     = "DUMC12T1",
  cnv_data   = cnv_df, sv_data = sv_df, wgd_data = wgd_df,
  karyotype  = "chr_info_hg38.rds",
  gene_coord = "gene.coord_strand_name.bed",
  outdir     = "plots"
)
}
}
\seealso{
\code{\link[=plot_sv_linear]{plot_sv_linear()}}
}
