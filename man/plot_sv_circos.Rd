% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sv_circos.R
\name{plot_sv_circos}
\alias{plot_sv_circos}
\title{Whole-genome SV circos plot for one sample}
\usage{
plot_sv_circos(
  sample,
  sv_data,
  cnv_data,
  wgd_data = NULL,
  genome = c("hg38", "hg19", "mm10"),
  bin_size = 1e+06,
  highlight_events = NULL,
  highlight_id_col = NULL,
  highlight_colour = "#d95f0e",
  dim_unhighlighted = FALSE,
  outdir = NULL,
  overwrite = TRUE
)
}
\arguments{
\item{sample}{Character scalar; the sample to plot (matched against the
\code{sample} column of \code{sv_data} / \code{cnv_data}).}

\item{sv_data}{A \code{data.frame}/\code{data.table} of structural variants in BEDPE
layout: \code{sample}, \code{chrom1}, \code{start1}, \code{chrom2}, \code{start2}, \code{svclass} (and
optionally \code{end1}/\code{end2}; when absent the start positions are used).}

\item{cnv_data}{A \code{data.frame}/\code{data.table} of copy-number segments: \code{sample},
\code{seqnames}, \code{start}, \code{end}, \code{copyNumber}, \code{ploidy}, \code{minorAlleleCopyNumber}.}

\item{wgd_data}{Optional \code{data.frame} with \code{sample} and \code{Polyploidy}
(\code{"wgd"} vs anything else) for the title; \code{NULL} labels the sample only.}

\item{genome}{Reference build; one of \code{"hg38"} (default), \code{"hg19"}, \code{"mm10"}.}

\item{bin_size}{Integer; genome tile width (bp) for the density and
copy-number rings (default \code{1e6}).}

\item{highlight_events}{Optional character vector of SV identifiers to draw
bold on top of the faint full-genome links (e.g. the junctions of one
chromoplexy cycle). Matched against \code{highlight_id_col}.}

\item{highlight_id_col}{Optional name of the column in \code{sv_data} holding the
identifiers matched by \code{highlight_events}; \code{NULL} (default) auto-detects
\code{name} then \code{event}.}

\item{highlight_colour}{Colour for highlighted links (default \code{"#d95f0e"}).}

\item{dim_unhighlighted}{Logical; when \code{TRUE}, non-highlighted links are greyed
so the highlighted set stands out (default \code{FALSE}).}

\item{outdir}{Optional directory; when supplied the plot is written to
\verb{<outdir>/<sample>_circos.pdf}. When \code{NULL} the plot is drawn on the current
graphics device (open one yourself, e.g. with \code{\link[grDevices:pdf]{grDevices::pdf()}}).}

\item{overwrite}{Logical; when \code{FALSE} and the output PDF already exists, the
sample is skipped (default \code{TRUE}).}
}
\value{
Invisibly \code{NULL}; called for its plotting side effect.
}
\description{
Draws a per-sample circos: chromosome-label + banded ideogram, a copy-number
status ring (amplification / gain / LOH / homozygous deletion / neutral,
relative to ploidy), separate intra- and inter-chromosomal breakpoint-density
rings, and structural-variant links coloured by class (DUP / TRA / h2hINV /
t2tINV / other). Titled with the sample and its whole-genome-doubling status.
}
\details{
A port of a bespoke circos plotter to EpiTracer conventions: hg38 by default,
the ideogram and genome tiling built from the bundled \code{chr_info}, and the
gUtils/regioneR helpers replaced by the package's own \code{\link[=to_granges]{to_granges()}} / \verb{\\\%$\\\%}.
\code{chrY} and \code{chrM} are dropped. Requires the \pkg{circlize} package.
}
\seealso{
\code{\link[=plot_sv_linear]{plot_sv_linear()}}
}
