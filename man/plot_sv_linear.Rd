% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sv_linear.R
\name{plot_sv_linear}
\alias{plot_sv_linear}
\title{Linear copy-number and structural rearrangement plot}
\usage{
plot_sv_linear(
  sample,
  cnv_data,
  sv_data,
  wgd_data = NULL,
  karyotype = system.file("extdata", "chr_info_hg38.rds", package = "EpiTracer"),
  gene_coord = system.file("extdata", "oncogene_coord_hg38.bed", package = "EpiTracer"),
  chromosome = NULL,
  chromosome_range = NULL,
  loci = NULL,
  events = "amp",
  cluster_gap = 5e+06,
  flank_pct = 10,
  min_cn_ratio = 3,
  gain_ratio = 1.4,
  loh_thresh = 0.5,
  homdel_thresh = 0.5,
  min_amp_width = 1e+05,
  gap_frac = 0.06,
  genes_to_highlight = NULL,
  gene_label_angle = NULL,
  repel_labels = TRUE,
  cn_max = NULL,
  displayExon = FALSE,
  cds_gr = NULL,
  offset_gene = 1.15,
  ymax_highlight_ratio = 1.08,
  karyotype_rel_size = 0.048,
  loh_position_ratio = 0.5,
  highlight_amp = TRUE,
  highlight_hom_del = TRUE,
  wgd_sample_col = NULL,
  snv_data = NULL,
  snv_sample_col = NULL,
  snv_y = c("imd", "vaf", "cn"),
  snv_type_col = NULL,
  vaf_col = "allelic_freq",
  snv_cn_col = "variant_cn",
  snv_rel_height = 0.4,
  snv_point_size = 0.7,
  snv_alpha = 0.6,
  snv_colour = "#1d3557",
  vaf_max = 1,
  outdir = NULL,
  save = TRUE,
  plot_width_custom = NULL,
  plot_height_custom = NULL,
  verbose = FALSE
)
}
\arguments{
\item{sample}{Character scalar; sample identifier (matched in \code{cnv_data},
\code{sv_data}, \code{wgd_data}).}

\item{cnv_data}{A data.frame of copy-number segments with columns \code{sample},
\code{seqnames}, \code{start}, \code{end}, \code{copyNumber}, \code{ploidy}, \code{majorAlleleCopyNumber},
\code{minorAlleleCopyNumber}.}

\item{sv_data}{A data.frame of SVs with columns \code{chrom1}, \code{start1}, \code{chrom2},
\code{start2}, \code{strand1}, \code{strand2}, \code{svclass}, \code{VF}, \code{JCN}, \code{sample}.}

\item{wgd_data}{Optional data.frame with a sample-identifier column (see
\code{wgd_sample_col}) and a \code{Polyploidy} column (\code{"No"} = diploid, otherwise
WGD). If supplied, the sample's WGD status is annotated in the plot title;
if \code{NULL} (default) the title shows just the sample name.}

\item{karyotype}{Ideogram bands (UCSC \code{cytoBand} / \code{chr_info} style), as a
data.frame (first column = chromosome; needs \code{start}, \code{end}, \code{gieStain}) or
an \code{.rds} path. Defaults to the bundled hg38 reference.}

\item{gene_coord}{Gene coordinates as a data.frame with columns
\code{chr},\code{start},\code{end},\code{strand},\code{gene}, or a path to a headerless tab-separated
BED-like file with those columns. Defaults to a small bundled hg38 table
covering the default oncogene panel; supply your own for other genes or
genome builds.}

\item{chromosome}{Optional character vector of chromosomes to display, e.g.
\code{c("chr7", "chr12")}.}

\item{chromosome_range}{Optional two-column matrix/data.frame of \code{start},\code{end}
window limits, one row per entry in \code{chromosome}.}

\item{loci}{Optional explicit loci: a \code{data.frame} with columns
\code{chr},\code{start},\code{end}, or a character vector of \code{"chr:start-end"} strings.
Takes precedence over \code{chromosome}/\code{chromosome_range}.}

\item{events}{Character vector of copy-number event types to target when
auto-detecting loci (used only when neither \code{loci} nor \code{chromosome_range} is
given). Any of: \code{"amp"} (amplification, \code{copyNumber > min_cn_ratio * ploidy}),
\code{"gain"} (\verb{> gain_ratio * ploidy} but not amplified), \code{"loh"}
(\code{minorAlleleCopyNumber < loh_thresh}), \code{"homdel"} (homozygous deletion,
\code{copyNumber < homdel_thresh}). Default \code{"amp"}. When explicit loci are
supplied the whole region is plotted regardless of event type -- the
function is a general CN/SV viewer, not amplification-only.}

\item{cluster_gap}{Numeric; when auto-detecting, consecutive event segments
more than this many bp apart start a new locus, so scattered focal events
become separate panels instead of one whole-chromosome span (default \code{5e6}).}

\item{flank_pct}{Numeric percentage by which each auto-detected (CN-status)
region is extended on both sides -- i.e. the flanking window shown around an
amplicon / deletion, as a percent of its width (default \code{10} = +/-10\%). Only
applies to auto-detected loci, not to explicit \code{chromosome}/\code{loci}.}

\item{min_cn_ratio}{Numeric; amplification threshold as a multiple of ploidy
(\code{copyNumber > min_cn_ratio * ploidy}, default \code{3}).}

\item{gain_ratio}{Numeric; gain threshold as a multiple of ploidy (default
\code{1.4}); a "gain" is \verb{> gain_ratio * ploidy} but not amplified.}

\item{loh_thresh}{Numeric; LOH minor-allele threshold
(\code{minorAlleleCopyNumber < loh_thresh}, default \code{0.5}).}

\item{homdel_thresh}{Numeric; homozygous-deletion copy-number threshold
(\code{copyNumber < homdel_thresh}, default \code{0.5}).}

\item{min_amp_width}{Numeric; drop auto-detected loci whose total event span
is below this many bp (default \code{1e5}).}

\item{gap_frac}{Numeric; gap between loci as a fraction of the total plotted
width (default \code{0.06}).}

\item{genes_to_highlight}{Optional character vector of gene symbols. If \code{NULL},
a default oncogene panel is used; only genes falling inside a locus window
are drawn.}

\item{gene_label_angle}{Optional numeric label rotation in degrees. If \code{NULL}
(default) labels are horizontal, switching to 45 degrees automatically when
genes are crowded.}

\item{repel_labels}{Logical; use \pkg{ggrepel} to de-collide gene labels
(default \code{TRUE}; falls back to plain labels if ggrepel is absent).}

\item{cn_max}{Optional numeric override for the copy-number axis top; if
\code{NULL} it is rounded up from the data to two significant figures.}

\item{displayExon}{Logical; if \code{TRUE}, draw exon models (from \code{cds_gr}) for
in-window genes instead of a point + label (default \code{FALSE}).}

\item{cds_gr}{Optional \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of CDS/exon ranges (metadata
column \code{gene_name}); required when \code{displayExon = TRUE}.}

\item{offset_gene, ymax_highlight_ratio, karyotype_rel_size, loh_position_ratio}{Layout tuning parameters.}

\item{highlight_amp, highlight_hom_del}{Logical; shade amplified / homozygously
deleted segments.}

\item{wgd_sample_col}{Optional name of the sample column in \code{wgd_data}
(default: \code{sample}, falling back to \code{WGS_ID}).}

\item{snv_data}{Optional SNV/SSM table as a \code{data.frame} or
\link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} with (at least) \code{seqnames}/\code{start} position columns,
a sample-identifier column (see \code{snv_sample_col}) and, for \code{snv_y = "vaf"}, a
variant-allele-frequency column (see \code{vaf_col}). When supplied, a second SNV
panel (intermutation-distance rainfall by default, or VAF; see \code{snv_y}) is
drawn directly beneath the copy-number / SV panel, sharing the same
concatenated genomic x-axis, and the two are returned/saved as one stacked
figure (requires the \pkg{patchwork} package). When \code{NULL} (default) only the
CN/SV panel is drawn and behaviour is unchanged.}

\item{snv_sample_col}{Optional name of the sample column in \code{snv_data}
(default: \code{sampleID}, falling back to \code{sample}).}

\item{snv_y}{What the SNV panel's y-axis shows: \code{"imd"} (default) plots the
intermutation distance -- the bp distance to the previous SNV on the same
chromosome, a rainfall plot, on a log10 axis; \code{"vaf"} plots the
variant-allele frequency; \code{"cn"} plots the SNV copy number (mutation copy
number, see \code{snv_cn_col}) -- in an amplicon this times each SNV against the
amplification. Only single-nucleotide variants are used in every case (indels
/ MNVs are excluded, see \code{snv_type_col}); intermutation distances are computed
across all of the sample's SNVs per chromosome before restricting to the
plotted loci, so window-edge mutations keep their true neighbour distance.}

\item{snv_type_col}{Optional name of a mutation-type column in \code{snv_data} used
to keep SNVs only (rows where the value is \code{"SNV"}). Defaults to \code{type} when
present; if no such column exists, SNVs are inferred from single-base
\code{ref}/\code{mut} columns.}

\item{vaf_col}{Name of the VAF column in \code{snv_data} (default \code{allelic_freq}),
used when \code{snv_y = "vaf"}. Values outside \verb{[0, vaf_max]} are treated as
artefacts and dropped.}

\item{snv_cn_col}{Name of the SNV copy-number column in \code{snv_data} (default
\code{variant_cn}), used when \code{snv_y = "cn"}.}

\item{snv_rel_height}{Numeric; height of the SNV panel relative to the CN/SV
panel (default \code{0.4}).}

\item{snv_point_size, snv_alpha, snv_colour}{Point size, alpha and colour for the
SNV VAF scatter (defaults \code{0.7}, \code{0.6}, \code{"#1d3557"}).}

\item{vaf_max}{Numeric top of the VAF axis; also the upper bound of the plausible
VAF window used to drop artefactual values (default \code{1}).}

\item{outdir}{Optional directory in which to write the plot. If \code{NULL} no file
is written and only the plot object is returned.}

\item{save}{Logical; if \code{TRUE} (default) and \code{outdir} is supplied, write the
plot as a PDF.}

\item{plot_width_custom, plot_height_custom}{Optional numeric overrides for the
output dimensions (inches).}

\item{verbose}{Logical; print progress/diagnostic messages.}
}
\value{
The assembled \link[ggplot2:ggplot]{ggplot2::ggplot} object (invisibly when a file is
written); written paths are attached as attribute \code{"path"}.
}
\description{
Draws allele-specific copy number, structural variant arcs, karyotype
ideograms and gene labels for one or more loci laid out side-by-side on a
single concatenated x-axis. Because all loci share one coordinate system, the
structural variants that interconnect separate amplicons (e.g. the junctions
of a multi-fragment / hub ecDNA) are drawn as arcs spanning the loci.
}
\details{
The loci to display are resolved in this order:
\enumerate{
\item \code{loci} if supplied (explicit windows);
\item \code{chromosome} + \code{chromosome_range} (explicit windows, one per
chromosome);
\item \code{chromosome} alone -- the amplified region on each named chromosome is
auto-detected and padded by \code{flank_pct}; a chromosome with no amplification
falls back to its whole length;
\item nothing -- every amplified locus in the sample is detected
automatically.
}
A locus is "amplified" where \code{copyNumber > min_cn_ratio * ploidy}; its
extent is the min-max of amplified segments on the chromosome, padded by
\code{flank_pct}, with single-segment artefacts below \code{min_amp_width} dropped.
}
\examples{
\dontrun{
# Single focused locus:
plot_sv_linear("DO11441T1", cnv, sv, wgd, karyotype = K, gene_coord = G,
               chromosome = "chr7",
               chromosome_range = matrix(c(52e6, 56e6), nrow = 1),
               outdir = "plots")

# All amplified loci, interconnected (multi-fragment ecDNA hub):
plot_sv_linear("DUMC12T1", cnv, sv, wgd, karyotype = K, gene_coord = G,
               outdir = "plots")

# Explicit loci:
plot_sv_linear("S1", cnv, sv, wgd, karyotype = K, gene_coord = G,
               loci = c("chr4:50e6-64e6", "chr12:57e6-59e6"))
}
}
\seealso{
\code{\link[=call_episomal_ecdna]{call_episomal_ecdna()}}
}
