% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sv_reconstruction.R
\name{plot_sv_reconstruction}
\alias{plot_sv_reconstruction}
\title{Temporally-stratified rearrangement plot (SV+CN grouped by read support)}
\usage{
plot_sv_reconstruction(
  sample,
  cnv_data,
  sv_data,
  wgd_data = NULL,
  genome = c("hg38", "hg19", "mm10"),
  karyotype = NULL,
  gene_coord = NULL,
  chromosome = NULL,
  chromosome_range = NULL,
  loci = NULL,
  events = "amp",
  cluster_gap = 5e+06,
  flank_pct = 10,
  min_cn_ratio = 3,
  gain_ratio = 1.4,
  loh_thresh = 0.5,
  homdel_thresh = 0.5,
  min_amp_width = 1e+05,
  gap_frac = 0.06,
  vf_col = "VF",
  k = "auto",
  max_k = 4,
  vf_breaks = NULL,
  min_vf = 1,
  isolate_founder = TRUE,
  founder_offscale = FALSE,
  vf_scale = c("shared", "per_panel"),
  cn_near_flank = 1e+05,
  cn_display = c("reconstruct", "actual"),
  prior_sv_alpha = 0.15,
  founder_alpha = 0.5,
  cn_border_lines = TRUE,
  cn_border_min_step = NULL,
  drop_empty_strata = TRUE,
  panel_rel_height = 1.4,
  genes_to_highlight = NULL,
  gene_label_angle = NULL,
  repel_labels = TRUE,
  displayExon = FALSE,
  cds_gr = NULL,
  cn_max = NULL,
  offset_gene = 1.15,
  ymax_highlight_ratio = 1.08,
  karyotype_rel_size = 0.048,
  loh_position_ratio = 0.5,
  highlight_amp = TRUE,
  highlight_hom_del = TRUE,
  highlight_events = NULL,
  highlight_id_col = NULL,
  highlight_colour = "#d95f0e",
  dim_unhighlighted = FALSE,
  wgd_sample_col = NULL,
  outdir = NULL,
  save = TRUE,
  plot_width_custom = NULL,
  plot_height_custom = NULL,
  verbose = FALSE
)
}
\arguments{
\item{sample}{Character scalar; sample identifier (matched in \code{cnv_data},
\code{sv_data}, \code{wgd_data}).}

\item{cnv_data}{A data.frame of copy-number segments with columns \code{sample},
\code{seqnames}, \code{start}, \code{end}, \code{copyNumber}, \code{ploidy}, \code{majorAlleleCopyNumber},
\code{minorAlleleCopyNumber}.}

\item{sv_data}{A data.frame of SVs with columns \code{chrom1}, \code{start1}, \code{chrom2},
\code{start2}, \code{strand1}, \code{strand2}, \code{svclass}, \code{VF}, \code{JCN}, \code{sample}.}

\item{wgd_data}{Optional data.frame with a sample-identifier column (see
\code{wgd_sample_col}) and a \code{Polyploidy} column (\code{"No"} = diploid, otherwise
WGD). If supplied, the sample's WGD status is annotated in the plot title;
if \code{NULL} (default) the title shows just the sample name.}

\item{genome}{Genome build for the bundled references: \code{"hg38"} (default),
\code{"hg19"} or \code{"mm10"}. Selects the karyotype ideogram and oncogene panel used
when \code{karyotype} / \code{gene_coord} are not given. Ignored for either reference
that is supplied explicitly.}

\item{karyotype}{Ideogram bands (UCSC \code{cytoBand} / \code{chr_info} style), as a
data.frame (first column = chromosome; needs \code{start}, \code{end}, \code{gieStain}) or
an \code{.rds} path. \code{NULL} (default) uses the bundled reference for \code{genome}.}

\item{gene_coord}{Gene coordinates as a data.frame with columns
\code{chr},\code{start},\code{end},\code{strand},\code{gene}, or a path to a headerless tab-separated
BED-like file with those columns. \code{NULL} (default) uses the bundled oncogene
panel for \code{genome}; supply your own to label other genes.}

\item{chromosome}{Optional character vector of chromosomes to display, e.g.
\code{c("chr7", "chr12")}.}

\item{chromosome_range}{Optional two-column matrix/data.frame of \code{start},\code{end}
window limits, one row per entry in \code{chromosome}.}

\item{loci}{Optional explicit loci: a \code{data.frame} with columns
\code{chr},\code{start},\code{end}, or a character vector of \code{"chr:start-end"} strings.
Takes precedence over \code{chromosome}/\code{chromosome_range}.}

\item{events}{Character vector of copy-number event types to target when
auto-detecting loci (used only when neither \code{loci} nor \code{chromosome_range} is
given). Any of: \code{"amp"} (amplification, \code{copyNumber > min_cn_ratio * ploidy}),
\code{"gain"} (\verb{> gain_ratio * ploidy} but not amplified), \code{"loh"}
(\code{minorAlleleCopyNumber < loh_thresh}), \code{"homdel"} (homozygous deletion,
\code{copyNumber < homdel_thresh}). Default \code{"amp"}. When explicit loci are
supplied the whole region is plotted regardless of event type -- the
function is a general CN/SV viewer, not amplification-only.}

\item{cluster_gap}{Numeric; when auto-detecting, consecutive event segments
more than this many bp apart start a new locus, so scattered focal events
become separate panels instead of one whole-chromosome span (default \code{5e6}).}

\item{flank_pct}{Numeric percentage by which each auto-detected (CN-status)
region is extended on both sides -- i.e. the flanking window shown around an
amplicon / deletion, as a percent of its width (default \code{10} = +/-10\%). Only
applies to auto-detected loci, not to explicit \code{chromosome}/\code{loci}.}

\item{min_cn_ratio}{Numeric; amplification threshold as a multiple of ploidy
(\code{copyNumber > min_cn_ratio * ploidy}, default \code{3}).}

\item{gain_ratio}{Numeric; gain threshold as a multiple of ploidy (default
\code{1.4}); a "gain" is \verb{> gain_ratio * ploidy} but not amplified.}

\item{loh_thresh}{Numeric; LOH minor-allele threshold
(\code{minorAlleleCopyNumber < loh_thresh}, default \code{0.5}).}

\item{homdel_thresh}{Numeric; homozygous-deletion copy-number threshold
(\code{copyNumber < homdel_thresh}, default \code{0.5}).}

\item{min_amp_width}{Numeric; drop auto-detected loci whose total event span
is below this many bp (default \code{1e5}).}

\item{gap_frac}{Numeric; gap between loci as a fraction of the total plotted
width (default \code{0.06}).}

\item{vf_col}{Name of the read-support column in \code{sv_data} (default \code{"VF"}).}

\item{k}{Number of \code{VF} strata. \code{"auto"} (default) chooses the number of
clusters automatically (via \pkg{Ckmeans.1d.dp} BIC when available, otherwise
a within-cluster-sum-of-squares elbow capped at \code{max_k}); an integer forces
that many. Reduced automatically when a sample has fewer distinct \code{VF} values
than \code{k}.}

\item{max_k}{Upper bound on the number of strata when \code{k = "auto"} (default
\code{4}).}

\item{vf_breaks}{Optional numeric vector of explicit \code{VF} cut points (upper
edges), e.g. \code{c(200, 80)} for strata \verb{>200}, \code{80-200}, \verb{<80}. Overrides \code{k}.}

\item{min_vf}{Junctions with \code{VF < min_vf} are dropped before clustering
(default \code{1}).}

\item{isolate_founder}{Logical (default \code{TRUE}); pull the single highest-\code{VF}
junction (the defining excision) out of the clustering and give it its own
top panel, headed "Max VF". Its arc is drawn at the same weight as every
other junction. It is excluded from the k-means / breaks step (so one
dominant outlier no longer skews the clusters), and the remaining strata are
headed "Cluster 1..X" (highest \code{VF} first). Ties at the maximum \code{VF} are
treated as one group. See \code{founder_offscale} for how it relates to the axis.}

\item{founder_offscale}{Logical (default \code{FALSE}); controls the shared
read-support axis when a founder is isolated. \code{FALSE}: the axis reflects the
true maximum \code{VF} (founder included), so the founder arc's height is literal.
\code{TRUE}: the axis is driven by the highest non-founder junction (so the lower
strata are not compressed by a large founder outlier) and the founder arc is
clamped to the axis top -- its true \code{VF} is still labelled, and every panel's
read-support axis title is marked "(scaled)" to flag that the axis no longer
maps 1:1 to \code{VF}. Preferred when the founder \code{VF} greatly exceeds the next
highest.}

\item{vf_scale}{How arc heights (and the right-hand read-support axis) are
scaled. \code{"shared"} (default) puts every panel on one global
read-support-to-copy-number scale, so arc heights are directly comparable
across strata -- best for multi-locus / hub amplicons. \code{"per_panel"} scales
each panel to its own stratum's \code{VF} range, so a single very-high-\code{VF}
junction does not crush the dynamic range of the lower strata -- usually the
better choice for a dense single-chromosome amplicon. The left copy-number
axis and the x-axis stay shared either way, so panels remain aligned.}

\item{cn_near_flank}{Numeric; in \code{cn_display = "actual"}, a copy-number
segment is drawn in a panel if it lies within this many bp of any of that
stratum's breakpoints (and, for intra-locus junctions, if it lies between
the two breakpoints). Default \code{1e5}.}

\item{cn_display}{How the major-allele copy number is drawn per panel.
\code{"reconstruct"} (default) shows an \emph{iterative} reconstruction of how the
copy-number profile is built up wave by wave. The breakpoints of the
junctions introduced so far (strata \eqn{1..k}) partition each locus into
intervals, and every interval is drawn flat at the minimum of the two
copy-number segments immediately adjacent to (just inside) its bounding
breakpoints. The founder panel therefore shows a single flat baseline over
the founder span; each lower panel introduces more breakpoints, so the
intervals subdivide and copy-number structure emerges; and the bottom panel
is the full observed bulk copy number that the reconstruction evolves
toward. This is a visual model, not a formal amplicon deconvolution.
\code{"actual"} instead draws the real observed CN segments near each stratum's
breakpoints in every panel (see \code{cn_near_flank}). The minor-allele CN is
always drawn in full regardless of this setting. In \code{"reconstruct"} the
allele-loss bars (LOH where minor CN \verb{< loh_thresh}, and homozygous
deletions where total CN \verb{< homdel_thresh}) are placed on the same timeline:
a loss segment is revealed in the wave of the earliest intra-locus deletion
junction whose deleted span covers it, or -- if no deletion covers it -- from
the founder wave down (treated as pre-existing).}

\item{prior_sv_alpha}{Numeric in \verb{[0, 1]}; opacity at which the junctions of
earlier (higher-\code{VF}) strata are re-drawn faintly in each lower panel, so
the accumulation of rearrangements is visible going down. The current
stratum is always drawn at full opacity. Set to \code{0} to show only the current
stratum's junctions in each panel (default \code{0.15}).}

\item{founder_alpha}{Numeric in \verb{[0, 1]}; opacity at which the founder
junction (the defining, highest-\code{VF} event) is re-drawn as a prior in the
lower panels. Kept higher than \code{prior_sv_alpha} so the founder stays
trackable all the way down the reconstruction, while later waves fade more.
The founder arc is also drawn with a slightly bolder line than the other
junctions. Has no effect on the founder's own (top) panel, where it is
always at full opacity (default \code{0.5}).}

\item{cn_border_lines}{Logical (default \code{TRUE}); draw vertical dashed guide
lines at the borders of the final (fully reconstructed) copy-number
segments. Because the panels share the x-axis, the guides line up across the
whole stack, marking where copy-number transitions occur.}

\item{cn_border_min_step}{Numeric minimum copy-number change for an \emph{internal}
border line to be drawn (each locus's outer edges are always drawn). If
\code{NULL} (default) a proportional threshold (\code{max(0.25, 0.015 * cn_axis_max)})
is used, so a guide is drawn at essentially every perceptible copy-number
step (only flat, equal-level segment splits are skipped). Set larger to show
only the biggest transitions, smaller to also mark sub-copy noise.}

\item{drop_empty_strata}{Logical; drop strata that end up with no drawable
in-locus junction (default \code{TRUE}).}

\item{panel_rel_height}{Numeric relative height of the (label-bearing) bottom
panel versus the others (default \code{1.4}).}

\item{genes_to_highlight}{Optional character vector of gene symbols. If \code{NULL},
a default oncogene panel is used; only genes falling inside a locus window
are drawn.}

\item{gene_label_angle}{Optional numeric label rotation in degrees. If \code{NULL}
(default) labels are horizontal, switching to 45 degrees automatically when
genes are crowded.}

\item{repel_labels}{Logical; use \pkg{ggrepel} to de-collide gene labels
(default \code{TRUE}; falls back to plain labels if ggrepel is absent).}

\item{displayExon}{Logical; if \code{TRUE}, draw exon models (from \code{cds_gr}) for
in-window genes instead of a point + label (default \code{FALSE}).}

\item{cds_gr}{Optional \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of CDS/exon ranges (metadata
column \code{gene_name}); required when \code{displayExon = TRUE}.}

\item{cn_max}{Optional numeric override for the copy-number axis top; if
\code{NULL} it is rounded up from the data to two significant figures, with a
floor of 2 so near-diploid regions still use a full 0-2 axis.}

\item{offset_gene, ymax_highlight_ratio, karyotype_rel_size, loh_position_ratio}{Layout tuning parameters.}

\item{highlight_amp, highlight_hom_del}{Logical; shade amplified / homozygously
deleted segments.}

\item{highlight_events}{Optional character vector of SV identifiers to draw
bold on top of the rest (e.g. the junctions of one chromoplexy cycle, a TBA
boundary, a fold-back set). Matched against \code{highlight_id_col}.}

\item{highlight_id_col}{Optional name of the column in \code{sv_data} holding the
identifiers matched by \code{highlight_events}; \code{NULL} (default) auto-detects
\code{name} then \code{event}.}

\item{highlight_colour}{Colour for highlighted SVs (default \code{"#d95f0e"}).}

\item{dim_unhighlighted}{Logical; when \code{TRUE}, non-highlighted SVs are greyed
so the highlighted set stands out (default \code{FALSE}).}

\item{wgd_sample_col}{Optional name of the sample column in \code{wgd_data}
(default: \code{sample}, falling back to \code{WGS_ID}).}

\item{outdir}{Optional directory in which to write the plot. If \code{NULL} no file
is written and only the plot object is returned.}

\item{save}{Logical; if \code{TRUE} (default) and \code{outdir} is supplied, write the
plot as a PDF.}

\item{plot_width_custom, plot_height_custom}{Optional numeric overrides for the
output dimensions (inches).}

\item{verbose}{Logical; print progress/diagnostic messages.}
}
\value{
A \pkg{patchwork} object stacking the per-stratum panels (invisibly
when a file is written); the written path is attached as attribute \code{"path"},
and the per-junction stratum assignment as attribute \code{"strata"}.
}
\description{
A companion to \code{\link[=plot_sv_linear]{plot_sv_linear()}} that decomposes a locus's structural
rearrangements into read-support (variant-fraction, \code{VF}) strata and draws one
stacked panel per stratum. Junctions with the highest read support -- taken as
a proxy for the earliest / highest-copy events -- are plotted at the top, then
successively lower-\code{VF} strata below, so the figure reads as a rough temporal
decomposition of amplicon evolution.
}
\details{
Strata are found by one-dimensional clustering of \code{log(VF)} (k-means; the
optimal 1-D solver \pkg{Ckmeans.1d.dp} is used when installed, otherwise
\link[stats:kmeans]{stats::kmeans}). \code{VF} here is the junction read-support count (as emitted by
PURPLE), which is strongly right-skewed, so clustering is done on the log scale
-- equal-width \code{VF} bins would collapse almost every junction into the lowest
bracket. All panels share the same concatenated-locus x-axis and the same
copy-number / read-support y-scaling, so arc heights are comparable across
strata; each panel draws only the copy-number segments lying near that
stratum's breakpoints, emphasising the CN change each set of events bounds.

Loci are resolved exactly as in \code{\link[=plot_sv_linear]{plot_sv_linear()}} (explicit \code{loci};
\code{chromosome} + \code{chromosome_range}; \code{chromosome} alone; or auto-detection of all
amplified loci).
}
\examples{
\dontrun{
plot_sv_reconstruction("DUMC12T1", cnv, sv, wgd, karyotype = K, gene_coord = G,
              k = "auto", outdir = "plots")

# Fixed read-support brackets:
plot_sv_reconstruction("DUMC12T1", cnv, sv, karyotype = K, gene_coord = G,
              vf_breaks = c(200, 80))
}
}
\seealso{
\code{\link[=plot_sv_linear]{plot_sv_linear()}}, \code{\link[=call_simple_excision]{call_simple_excision()}}
}
