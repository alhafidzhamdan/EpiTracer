% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-inputs.R
\name{prepare_amplicon_inputs}
\alias{prepare_amplicon_inputs}
\title{Build EpiTracer caller inputs from copy-number and SV tables}
\usage{
prepare_amplicon_inputs(
  cnv,
  sv,
  sample = NULL,
  ploidy_mode = c("per_chr", "global")
)
}
\arguments{
\item{cnv, sv}{Copy-number and SV tables (from \code{\link[=read_cnv]{read_cnv()}} / \code{\link[=read_sv]{read_sv()}}).}

\item{sample}{Sample to build for. Defaults to the first CN sample.}

\item{ploidy_mode}{\code{"per_chr"} (default) sets each breakend/segment ploidy to the
local per-chromosome baseline (recovers amplicons on polysomic chromosomes);
\code{"global"} uses the CN table's own ploidy.}
}
\value{
A list with \code{cnv_gr} and \code{breakpoints_gr}.
}
\description{
Assembles the \code{cnv_gr} and \code{breakpoints_gr} \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} that the
amplicon-mechanism callers (\code{\link[=call_simple_excision]{call_simple_excision()}}, \code{\link[=call_brf]{call_brf()}}, etc.) take,
from the tables returned by \code{\link[=read_cnv]{read_cnv()}} / \code{\link[=read_sv]{read_sv()}}. If the CN and SV tables
use different sample labels (common with PURPLE file naming), pass \code{sample} to
force a single name; the SVs are then taken from the whole SV table.
}
\seealso{
\code{\link[=read_cnv]{read_cnv()}}, \code{\link[=read_sv]{read_sv()}}, \code{\link[=call_simple_excision]{call_simple_excision()}}
}
