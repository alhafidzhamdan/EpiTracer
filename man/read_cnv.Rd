% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-inputs.R
\name{read_cnv}
\alias{read_cnv}
\title{Read and reformat copy-number segments for EpiTracer}
\usage{
read_cnv(x, sample = NULL, ploidy = NULL, name = NULL)
}
\arguments{
\item{x}{A file path (TSV/CSV, \verb{*.purple.cnv.somatic.tsv}, or a \code{.rds} of a
GRanges/data.frame) or an in-memory data.frame/data.table.}

\item{sample}{Optional sample name. When given it \strong{overrides} any \code{sample}
column and the file name, labelling every row with this name -- use it to give
CN and SV files a common name when their file-naming differs.}

\item{ploidy}{Optional numeric ploidy. When given it overrides the estimate.}

\item{name}{Optional file name used for format detection and to derive the sample
when \code{x} is a temporary path (e.g. a Shiny upload) whose own name is opaque.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} in EpiTracer's copy-number format.
}
\description{
Turns a copy-number table from (almost) any source into the columns EpiTracer's
plotters and callers expect: \code{sample}, \code{seqnames}, \code{start}, \code{end}, \code{copyNumber},
\code{ploidy}, \code{majorAlleleCopyNumber}, \code{minorAlleleCopyNumber}. Column names are
matched case-insensitively, so a raw PURPLE \verb{*.purple.cnv.somatic.tsv} works
directly. PURPLE's occasional negative copy numbers are clamped to 0; the allele
copy numbers are derived if absent; and, because a PURPLE segment file carries no
ploidy (it lives in \verb{*.purple.purity.tsv}), ploidy is estimated from the segments
unless supplied.
}
\examples{
\dontrun{
cnv <- read_cnv("DO13869T.purple.cnv.somatic.tsv", sample = "DO13869T")
}
}
