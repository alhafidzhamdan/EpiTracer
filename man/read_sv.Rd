% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-inputs.R
\name{read_sv}
\alias{read_sv}
\title{Read and reformat structural variants for EpiTracer}
\usage{
read_sv(x, sample = NULL, name = NULL)
}
\arguments{
\item{x}{A file path (BEDPE TSV/CSV, \verb{*.vcf}/\verb{*.vcf.gz}, or \code{.rds}) or a
data.frame/data.table.}

\item{sample}{Optional sample name; overrides any \code{sample} column and the file
name (see \code{\link[=read_cnv]{read_cnv()}}).}

\item{name}{Optional file name for format detection / sample derivation.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} in EpiTracer's SV (BEDPE) format.
}
\description{
Accepts a BEDPE (with \code{VF}/\code{JCN}), a PURPLE/GRIDSS breakend VCF (parsed by
\code{\link[=read_purple_sv_vcf]{read_purple_sv_vcf()}}), or a \code{.rds}, and returns the columns EpiTracer uses:
\code{chrom1}, \code{start1}, \code{chrom2}, \code{start2}, \code{strand1}, \code{strand2}, \code{svclass}, \code{VF},
\code{JCN}, \code{name}, \code{homlen}, \code{sample}. \code{svclass} is normalised to
\code{DEL}/\code{DUP}/\code{INS}/\code{TRA}/\code{h2hINV}/\code{t2tINV}. Keeping \strong{VF} (supporting fragments)
and \strong{JCN} (junction copy number) matters -- the mechanism callers use them.
}
