% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-amplicon.R
\name{seed_locus}
\alias{seed_locus}
\title{A seed locus for simulation}
\usage{
seed_locus(gene, genome = c("hg38", "hg19", "mm10"), flank = 250000)
}
\arguments{
\item{gene}{Gene symbol(s), e.g. \code{"EGFR"} or \code{c("CDK4", "MDM2")}.}

\item{genome}{One of \code{"hg38"}, \code{"hg19"}, \code{"mm10"}.}

\item{flank}{Bp of neighbouring sequence co-excised with the gene
(default \code{2.5e5}, giving a sub-megabase episome typical of a simple
excision).}
}
\value{
A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of the seed region(s), with a \code{preserved}
attribute holding the gene spans and a \code{gene} metadata column.
}
\description{
Convenience wrapper that resolves an oncogene symbol to the amplicon seed and
preserved-region pair that \code{\link[=sim_episome]{sim_episome()}} expects, using the oncogene panel
bundled with EpiTracer. The returned region is the gene span padded by \code{flank}
-- a plausible excised neighbourhood -- and carries the gene itself as the
locus that must be preserved through shattering.
}
\examples{
seed_locus("EGFR")
attr(seed_locus("EGFR"), "preserved")
}
\seealso{
\code{\link[=sim_episome]{sim_episome()}}, \code{\link[=gene_locus]{gene_locus()}}
}
