% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-cohort.R
\name{sim_cohort}
\alias{sim_cohort}
\title{Simulate a cohort of ecDNA amplicons with known mechanisms}
\usage{
sim_cohort(
  n = 30,
  mixture = c(episomal = 1, chromothriptic = 1, chimeric = 1),
  genes = .sim_default_genes,
  rounds = 1:3,
  n_breaks = 8:30,
  copies = c(15, 80),
  host_ploidy = c(2, 2, 2, 3),
  homology = c("nhej", "nhej", "mmej"),
  noise = sim_noise(),
  prefix = "SIM",
  seed = NULL,
  mc.cores = 1L
)
}
\arguments{
\item{n}{Number of samples (default \code{30}).}

\item{mixture}{Named numeric vector of class weights, normalised internally.
Recognised names are \code{episomal} (born and unshattered), \code{chromothriptic}
(one or more micronucleation rounds) and \code{chimeric} (two episomes fused).
Defaults to an even split.}

\item{genes}{Character vector of oncogene symbols to draw seed loci from
(default: a panel of recurrently ecDNA-amplified oncogenes).}

\item{rounds}{Integer vector of micronucleation rounds to sample from for the
\code{chromothriptic} class (default \code{1:3}).}

\item{n_breaks}{Integer vector of chromothriptic breakpoint counts to sample
from (default \code{8:30}).}

\item{copies}{Numeric length-2 range of ecDNA copies per cell at birth
(default \code{c(15, 80)}).}

\item{host_ploidy}{Numeric vector of host baseline ploidies to sample from
(default \code{c(2, 2, 2, 3)}, so a quarter of samples sit on a polysomic
chromosome -- the case that defeats a ploidy-based flank test).}

\item{homology}{Repair pathways to sample the circularisation junction from
(default \code{c("nhej", "nhej", "mmej")}).}

\item{noise}{A \code{\link[=sim_noise]{sim_noise()}} specification, or a numeric scale.}

\item{prefix}{Sample-name prefix (default \code{"SIM"}).}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}}.}

\item{mc.cores}{Cores for \code{\link[parallel:mclapply]{parallel::mclapply()}} (default \code{1}).}
}
\value{
A named list: the four caller inputs (\code{ecdna_gr}, \code{breakpoints_gr},
\code{cnv_gr}, \code{cancer_genes_gr}), a \code{truth} \link[data.table:data.table]{data.table::data.table} with one
row per simulated amplicon, and \code{sims}, the list of underlying \link{ecdna_sim}
objects (so any sample can be re-rendered at different noise, or plotted).
}
\description{
Draws \code{n} independent samples from a mixture of ecDNA trajectories and renders
them as one set of EpiTracer inputs with a ground-truth label per sample. The
three trajectory classes correspond to the questions the suite was built to
answer: what a \strong{freshly born} episome looks like, what it looks like after
\strong{one or several rounds} of micronucleation and chromothripsis, and what a
\strong{chimera} of two co-encapsulated episomes looks like.
}
\examples{
co <- sim_cohort(n = 6, seed = 1)
co$truth[, .(WGS_ID, class, rounds, chimeric, truth_max_cn)]
}
\seealso{
\code{\link[=sim_episome]{sim_episome()}}, \code{\link[=sim_evolve]{sim_evolve()}}, \code{\link[=sim_fuse_episomes]{sim_fuse_episomes()}}, \code{\link[=sim_benchmark]{sim_benchmark()}}
}
