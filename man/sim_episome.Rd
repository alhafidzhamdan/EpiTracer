% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-mechanisms.R
\name{sim_episome}
\alias{sim_episome}
\title{Simulate the birth of an episomal ecDNA}
\usage{
sim_episome(
  regions,
  sample = "SIM01",
  copies = 40,
  preserved = NULL,
  host_ploidy = 2,
  host_flank = 1.5e+07,
  scar = TRUE,
  retain_source = TRUE,
  homology = c("nhej", "mmej", "nahr"),
  genome = "hg38",
  seed = NULL
)
}
\arguments{
\item{regions}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} of the region(s) to excise. Several
regions produce a circle assembled from several segments, as when an
episome forms from a locus already broken by internal deletions. Use
\code{\link[=seed_locus]{seed_locus()}} to build one from a gene symbol.}

\item{sample}{Character sample identifier carried into the emitted inputs.}

\item{copies}{Numeric ecDNA copies per tumour cell (default \code{40}). Copy number
is \code{copies * coverage(fragments)} over the host baseline, so this sets the
amplification level; patient ecDNA amplicons typically sit at 20-100.}

\item{preserved}{Oncogene loci that later shattering must not delete. Defaults
to the \code{preserved} attribute of \code{regions} when \code{\link[=seed_locus]{seed_locus()}} made it, else
to \code{regions} themselves.}

\item{host_ploidy}{Numeric baseline copy number of the host chromosome
(default \code{2}). Set higher to model an episome on a polysomic chromosome --
e.g. \code{3} for the chr7 gain that accompanies EGFR ecDNA in glioblastoma.}

\item{host_flank}{Bp of host chromosome to represent on each side of the
amplicon (default \code{1.5e7}), giving the flanks the callers test.}

\item{scar}{Logical; emit the chromosomal excision scar (default \code{TRUE}). Set
\code{FALSE} to model an episome whose source chromosome was lost, so the scar is
unobservable.}

\item{retain_source}{Logical; does the source chromosome keep the excised
region on its other allele (default \code{TRUE})? When \code{TRUE} the excised
interval sits one copy below the flanks on the chromosome, the usual
single-allele excision.}

\item{homology}{Repair pathway that sealed the circularisation junction, one
of \code{"nhej"} (blunt), \code{"mmej"} (2-10 bp microhomology) or \code{"nahr"}
(>=14 bp homology). Sets \code{HOMLEN} on the emitted breakends, the
breakpoint-homology signature \code{call_simple_excision()} reports.}

\item{genome}{Genome build for chromosome lengths (default \code{"hg38"}).}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}} for reproducibility.}
}
\value{
An \link{ecdna_sim} object.
}
\description{
Creates a simple-excision episome: one or more genomic regions are excised
from the chromosome and circularised. This is the mechanism
\code{\link[=call_simple_excision]{call_simple_excision()}} is built to detect, and the simulated amplicon
carries its three signatures by construction --- a \strong{duplication-orientation
junction at the amplicon boundary} (the circularisation junction, \verb{-/+},
emitted as \code{svclass == "DUP"}), \strong{non-gained flanks} (the host chromosome
stays at its baseline ploidy immediately outside the amplicon), and an
\strong{excision scar} (a low-copy deletion junction joining the flanks on the
chromosome the circle left behind).
}
\examples{
ec <- sim_episome(seed_locus("EGFR"), sample = "SIM01", copies = 50)
ec
summary(ec)
}
\references{
Shoshani, O. \emph{et al.} Chromothripsis drives the evolution of gene
amplification in cancer. \emph{Nature} \strong{591}, 137-141 (2021).
}
\seealso{
\code{\link[=sim_micronucleation]{sim_micronucleation()}}, \code{\link[=sim_to_epitracer]{sim_to_epitracer()}}, \code{\link[=call_simple_excision]{call_simple_excision()}}
}
