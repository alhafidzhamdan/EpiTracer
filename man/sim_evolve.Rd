% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-mechanisms.R
\name{sim_evolve}
\alias{sim_evolve}
\title{Evolve a simulated ecDNA through several rounds}
\usage{
sim_evolve(x, rounds = 1L, keep_all = FALSE, ..., seed = NULL)
}
\arguments{
\item{x}{An \link{ecdna_sim}.}

\item{rounds}{Integer number of micronucleation + chromothripsis rounds.}

\item{keep_all}{Logical; return a list of \link{ecdna_sim} objects, one per round
including the input as element 1 (default \code{FALSE}, returning only the final
amplicon).}

\item{...}{Passed to \code{\link[=sim_micronucleation]{sim_micronucleation()}} (\code{n_breaks}, \code{del_p}, \code{inv_p},
\code{dup_p}, \code{amplify}, ...). A vector-valued \code{n_breaks} is recycled across
rounds, so \code{n_breaks = c(20, 5)} models a severe first shattering followed
by a milder one.}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}} once, before the first
round.}
}
\value{
An \link{ecdna_sim}, or a list of them when \code{keep_all = TRUE}.
}
\description{
Applies \code{\link[=sim_micronucleation]{sim_micronucleation()}} \code{rounds} times, so one call produces the whole
trajectory from a clean episome to a heavily rearranged amplicon. Returns the
final amplicon by default, or the full trajectory (\code{keep_all = TRUE}) so the
rounds can be compared --- the natural way to ask at what point EpiTracer stops
calling an amplicon episomal and starts calling it chromothriptic.
}
\examples{
ec <- sim_episome(seed_locus("EGFR"), sample = "SIM03")
traj <- sim_evolve(ec, rounds = 3, n_breaks = 12, keep_all = TRUE, seed = 1)
do.call(rbind, lapply(traj, summary))[, c("rounds", "n_junctions", "circle_mb")]
}
\seealso{
\code{\link[=sim_micronucleation]{sim_micronucleation()}}
}
