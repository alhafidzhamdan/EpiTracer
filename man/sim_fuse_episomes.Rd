% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-mechanisms.R
\name{sim_fuse_episomes}
\alias{sim_fuse_episomes}
\title{Simulate chimeric ecDNA formation by co-encapsulation of two episomes}
\usage{
sim_fuse_episomes(
  x,
  y,
  n_breaks = 10L,
  del_p = 0.15,
  inv_p = 0.5,
  dup_p = 0.2,
  max_dup = 3L,
  copies = NULL,
  sample = NULL,
  homology = c("nhej", "mmej", "nahr"),
  seed = NULL
)
}
\arguments{
\item{x, y}{Two \link{ecdna_sim} objects to co-encapsulate. \code{x} supplies the sample
name, host background and copy level unless overridden.}

\item{n_breaks}{Integer breakpoints applied to the pooled fragment set
(default \code{10}).}

\item{del_p, inv_p, dup_p, max_dup}{Fragment loss, inversion and reduplication,
as in \code{\link[=sim_micronucleation]{sim_micronucleation()}}.}

\item{copies}{Copies per cell of the chimera; defaults to the mean of the two
parents, the chimera replacing both.}

\item{sample}{Sample name for the chimera (defaults to \code{x}'s).}

\item{homology}{Repair pathway sealing the new junctions (default \code{"nhej"}).}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}}.}
}
\value{
An \link{ecdna_sim} whose fragments carry an \code{origin} column naming the
parental episome each came from, so the chimeric contribution is traceable.
}
\description{
Two independently born episomes are captured in the \strong{same} micronucleus,
shattered together, and religated into a single chimeric circle carrying
fragments of both --- the route by which an ecDNA acquires sequence from a
second, non-homologous locus.
}
\details{
When the two episomes come from different chromosomes the chimera necessarily
carries \strong{inter-chromosomal junctions with both breakends inside amplified
copy number}, which is precisely the signature \code{\link[=call_chimeric_amplicon]{call_chimeric_amplicon()}}
detects. It also puts two oncogenes on one circle, the co-amplification
(e.g. \emph{EGFR} with \emph{CDK4}, or \emph{MYC} with a distant enhancer) that is common in
patient amplicons and cannot arise from a single excision.
}
\section{Circle populations}{

A shattering event happens inside ONE micronucleus, to ONE molecule, and the
surviving product is what sweeps. So when the amplicon is a mixture of circle
species (see \code{\link[=sim_internal_deletion]{sim_internal_deletion()}}), this operator acts on the most
abundant species and the population collapses back to one --- the shattered
descendant. Minority species present before the event are not carried through.
}

\examples{
a <- sim_episome(seed_locus("EGFR"), sample = "SIM02", copies = 40)
b <- sim_episome(seed_locus("CDK4"), sample = "SIM02", copies = 30)
ch <- sim_fuse_episomes(a, b, seed = 1)
summary(ch)   # n_chr == 2, n_tra > 0
}
\references{
Hung, K. L. \emph{et al.} ecDNA hubs drive cooperative intermolecular
oncogene expression. \emph{Nature} \strong{600}, 731-736 (2021).
}
\seealso{
\code{\link[=sim_micronucleation]{sim_micronucleation()}}, \code{\link[=call_chimeric_amplicon]{call_chimeric_amplicon()}}
}
