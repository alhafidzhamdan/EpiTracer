% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-mechanisms.R
\name{sim_micronucleation}
\alias{sim_micronucleation}
\title{Simulate one round of micronucleation and chromothripsis}
\usage{
sim_micronucleation(
  x,
  n_breaks = 10L,
  del_p = 0.2,
  inv_p = 0.5,
  dup_p = 0.25,
  max_dup = 3L,
  amplify = 1.5,
  max_cn = 200,
  homology = c("nhej", "mmej", "nahr"),
  seed = NULL
)
}
\arguments{
\item{x}{An \link{ecdna_sim}, from \code{\link[=sim_episome]{sim_episome()}} or a previous round.}

\item{n_breaks}{Integer number of chromothriptic breakpoints (default \code{10}).
Patient chromothripsis footprints typically carry 10-50.}

\item{del_p}{Probability a non-preserved fragment is lost (default \code{0.2}).}

\item{inv_p}{Probability a surviving fragment is inverted (default \code{0.5}).}

\item{dup_p}{Per-trial probability of an extra incorporation of a fragment
(default \code{0.25}), giving the copy-number oscillation described above. Set
\code{0} for strict shatter-and-rejoin with no reduplication.}

\item{max_dup}{Maximum times one fragment may appear in the religated circle
(default \code{3}).}

\item{amplify}{Multiplicative change in copies per cell across the round
(default \code{1.5}), modelling selection for the fitter, oncogene-retaining
circle. Set \code{1} for no amplification.}

\item{max_cn}{Ceiling on the amplicon's peak copy number (default \code{200}).
Copies per cell are scaled back so \code{copies * max(coverage)} stays at or
below it, modelling the plateau beyond which further oncogene dosage buys no
selective advantage (the \code{N_fitness} saturation of the Bernhard \emph{et al.}
model). Without it, compounding \code{amplify} and fragment reduplication drive
copy number far past anything observed in patients.}

\item{homology}{Repair pathway sealing the new junctions (default \code{"nhej"};
micronuclear religation is canonically blunt-ended NHEJ).}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}}.}
}
\value{
An \link{ecdna_sim} with the religated fragment list, the accumulated
junction ledger and \code{round} incremented.
}
\description{
Encapsulates the ecDNA in a micronucleus, shatters it, and religates the
surviving fragments in random order and orientation back into a circle --- the
ecDNA -> micronucleus -> chromothripsis route by which a clean episome becomes
a complex amplicon.
}
\details{
Three things happen, in order:
\enumerate{
\item \strong{Shattering.} The circle is cut at \code{n_breaks} positions drawn uniformly
over its sequence (so wide fragments are cut more often than narrow ones).
\item \strong{Fragment loss and reduplication.} Each fragment is lost with probability
\code{del_p}, except fragments carrying a preserved oncogene, which always
survive --- a circle that loses its oncogene is not selected. Each surviving
fragment is incorporated \code{1 + rbinom(1, max_dup - 1, dup_p)} times. This
reduplication is what generates the \strong{oscillating copy-number profile}
that distinguishes a chromothriptic amplicon from a clean episome (which
would otherwise be a flat two-level profile), and it is the step
\code{\link[=call_chromothripsis]{call_chromothripsis()}} scores as \code{cn_oscillations}.
\item \strong{Random rejoining.} Fragments are shuffled and each is inverted with
probability \code{inv_p}, then religated into a circle. Because order and
orientation are random, the four intrachromosomal junction orientations
(\code{DEL}, \code{DUP}, \code{h2hINV}, \code{t2tINV}) come out close to equally represented ---
the \strong{random fragment joins} hallmark, which \code{\link[=call_chromothripsis]{call_chromothripsis()}} tests
with a chi-squared goodness-of-fit and which separates chromothripsis from
orientation-biased mechanisms such as BFB.
}

Applying this repeatedly (see \code{\link[=sim_evolve]{sim_evolve()}}) compounds the rearrangement:
junction counts grow roughly linearly in the number of rounds while the circle
contracts, reproducing the small, junction-dense amplicons seen in patients.
}
\section{Circle populations}{

A shattering event happens inside ONE micronucleus, to ONE molecule, and the
surviving product is what sweeps. So when the amplicon is a mixture of circle
species (see \code{\link[=sim_internal_deletion]{sim_internal_deletion()}}), this operator acts on the most
abundant species and the population collapses back to one --- the shattered
descendant. Minority species present before the event are not carried through.
}

\examples{
ec <- sim_episome(seed_locus("EGFR"), sample = "SIM01")
mn <- sim_micronucleation(ec, n_breaks = 12, seed = 1)
summary(mn)
}
\references{
Zhang, C.-Z. \emph{et al.} Chromothripsis from DNA damage in
micronuclei. \emph{Nature} \strong{522}, 179-184 (2015).
}
\seealso{
\code{\link[=sim_evolve]{sim_evolve()}}, \code{\link[=sim_fuse_episomes]{sim_fuse_episomes()}}, \code{\link[=call_chromothripsis]{call_chromothripsis()}}
}
