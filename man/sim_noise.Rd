% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-emit.R
\name{sim_noise}
\alias{sim_noise}
\title{Sequencing and calling noise for simulated amplicons}
\usage{
sim_noise(
  scale = 1,
  purity = 0.8,
  depth = 60,
  cn_sd = 0.25,
  cn_cv = 0.02,
  vf_cv = 0.1,
  dropout = 0.03,
  min_vf = 4,
  fp_rate = 0.5,
  min_seg_width = 10000,
  bp_jitter = 0
)
}
\arguments{
\item{scale}{Numeric multiplier applied to every stochastic term (default
\code{1}). \code{sim_noise(0)} is a noiseless, exact rendering of the ground truth;
\code{sim_noise(2)} roughly doubles the segmentation error, the read-support
dispersion and the junction dropout and false-positive rates.}

\item{purity}{Tumour purity (default \code{0.8}). Lowers junction variant allele
fraction and read support.}

\item{depth}{Mean sequencing depth in x (default \code{60}), the usual WGS target.
Read support for a junction scales with \code{depth * purity}.}

\item{cn_sd}{Copy-number segmentation error in copies, absolute component
(default \code{0.25}; PURPLE's segment-level error is typically 0.2-0.5).}

\item{cn_cv}{Proportional component of the segmentation error (default
\code{0.02}), so high-copy segments carry proportionally more error.}

\item{vf_cv}{\strong{Extra-Poisson} dispersion of junction read support (default
\code{0.1}). Read support is a fragment COUNT, so its dominant noise is counting
noise: support is drawn from a negative binomial whose variance is
\code{mu + (vf_cv * mu)^2}. A weakly supported junction is therefore noisy in
relative terms (at \code{mu = 12}, CV about 0.30, Poisson-dominated) while a
strongly supported one is tight (at \code{mu = 3000}, CV about 0.10, set by
\code{vf_cv}, which stands for local coverage variation -- GC, mappability).
Modelling this as a constant-CV log-normal instead would over-disperse
high-support junctions by an order of magnitude and hide the fact that
support is proportionate to junction copy number.}

\item{dropout}{Probability a true junction is missed, before the additional
support-dependent dropout of weakly supported junctions (default \code{0.03}).}

\item{min_vf}{Junctions whose simulated read support falls below this are
dropped as unsupported (default \code{4}), the usual caller floor.}

\item{fp_rate}{Expected number of false-positive junctions added per amplicon
(default \code{0.5}).}

\item{min_seg_width}{Copy-number segments narrower than this are merged into
their neighbours (default \code{1e4}), modelling the resolution limit of
read-depth segmentation -- without it a heavily shattered circle emits
hundreds of sub-kb segments no real caller would resolve.}

\item{bp_jitter}{Positional error on reported breakend coordinates, in bp
(default \code{0}; set e.g. \code{10} to model imprecise breakpoints).}
}
\value{
A named list of noise parameters, of class \code{sim_noise}.
}
\description{
Bundles the technical parameters \code{\link[=sim_to_epitracer]{sim_to_epitracer()}} uses to turn ground
truth into the imperfect observation a short-read pipeline reports. Passing a
scalar to \code{sim_noise()} scales every noise term at once, giving a one-knob
difficulty sweep; passing named arguments sets terms individually.
}
\examples{
sim_noise(0)          # exact ground truth
sim_noise(purity = 0.4, depth = 30)   # a low-purity, shallow sample
}
\seealso{
\code{\link[=sim_to_epitracer]{sim_to_epitracer()}}
}
