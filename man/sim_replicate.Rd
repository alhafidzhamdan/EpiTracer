% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-mechanisms.R
\name{sim_replicate}
\alias{sim_replicate}
\title{Simulate rounds of ecDNA amplification}
\usage{
sim_replicate(
  x,
  rounds = 1L,
  fold = 2,
  jitter = 0,
  max_cn = Inf,
  keep_all = FALSE,
  seed = NULL
)
}
\arguments{
\item{x}{An \link{ecdna_sim}, typically straight from \code{\link[=sim_episome]{sim_episome()}}.}

\item{rounds}{Integer number of replication rounds (default \code{1}).}

\item{fold}{Multiplicative gain in copies per round (default \code{2}, one
doubling). Non-integer values model partial selective sweeps. When the
amplicon is a mixture of circle species each grows by \code{fold * fitness}, so a
species with a replication advantage expands its share of the population.}

\item{jitter}{Log-normal dispersion applied to each round's fold change
(default \code{0}, i.e. deterministic). Set e.g. \code{0.15} to model uneven
segregation between rounds.}

\item{max_cn}{Ceiling on peak copy number (default \code{Inf}; see
\code{\link[=sim_micronucleation]{sim_micronucleation()}}).}

\item{keep_all}{Logical; return a list of \link{ecdna_sim} objects, one per round
including the input as element 1 (default \code{FALSE}).}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}}.}
}
\value{
An \link{ecdna_sim}, or a list of them when \code{keep_all = TRUE}.
}
\description{
Amplifies a circle \strong{without changing its structure}: the fragment list and
the junction ledger are untouched, and only \code{copies} (ecDNA molecules per
tumour cell) rises, by \code{fold} per round.
}
\details{
\code{fold} is a \strong{net growth factor of copy number under selection, not a cell
division}. Cell divisions do not raise ecDNA copy number: with no centromere
the copies partition at random between daughters, so replication doubles and
division halves, and the expected copy number per cell is unchanged. The
neutral value of \code{fold} is therefore \code{1}, and \code{fold = 2} is an aggressive
selective sweep. Use \code{\link[=sim_segregate]{sim_segregate()}} to let the level follow from a number
of generations and a selection coefficient instead of setting it by hand.

Because junction copy number is \code{multiplicity * copies}, and simulated read
support is proportional to junction copy number, the effect on the emitted
data is that the \strong{boundary duplication gains read support round after round}
while no new junction appears. That is the signature of a clean episome
amplifying: one junction, rising \code{VF}, \code{PURPLE_JCN} and \code{PURPLE_CN}, flanks
unchanged at the host baseline.

Contrast \code{\link[=sim_micronucleation]{sim_micronucleation()}}, which changes the structure (new junctions,
oscillating copy number) rather than only the level.
}
\examples{
ec <- sim_episome(seed_locus("EGFR"), sample = "REP01", copies = 4)
traj <- sim_replicate(ec, rounds = 5, fold = 2, keep_all = TRUE)

## structure is fixed; only the level rises
do.call(rbind, lapply(traj, summary))[, c("copies", "max_cn", "n_junctions")]
}
\seealso{
\code{\link[=sim_episome]{sim_episome()}}, \code{\link[=sim_micronucleation]{sim_micronucleation()}}, \code{\link[=sim_to_epitracer]{sim_to_epitracer()}}
}
