% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-mechanisms.R
\name{sim_segregate}
\alias{sim_segregate}
\title{Grow an ecDNA population by segregation and selection}
\usage{
sim_segregate(
  x,
  generations = 20L,
  selection = 0.05,
  plateau = 40,
  n_cells = 2000L,
  addicted = TRUE,
  max_cn = Inf,
  seed = NULL
)
}
\arguments{
\item{x}{An \link{ecdna_sim}.}

\item{generations}{Integer number of cell divisions to run (default \code{20}).}

\item{selection}{Dosage-selection coefficient (default \code{0.05}). \code{0} is
neutral; the population mean copy number then does not rise, however many
generations elapse.}

\item{plateau}{Copy number above which extra dosage buys no further advantage
(default \code{40}), the saturation seen in ecDNA fitness models.}

\item{n_cells}{Number of cells simulated (default \code{2000}). Larger is smoother
and slower; the returned copy number is a population mean, so a few thousand
cells is ample.}

\item{addicted}{Logical; do cells that lose the amplicon entirely die
(default \code{TRUE}, oncogene addiction)?}

\item{max_cn}{Ceiling on peak copy number (default \code{Inf}).}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}}.}
}
\value{
An \link{ecdna_sim} with \code{copies} set to the evolved population mean. The
per-cell copy-number distribution is attached as the \code{"cells"} attribute,
for single-cell or heterogeneity work; bulk rendering ignores it. Species
proportions are rescaled together (co-segregation of distinct circle species
within a cell is not modelled).
}
\description{
Evolves the amplicon's copy number over \code{generations} of cell division under
an explicit model of how ecDNA actually propagates: each S phase every circle
replicates, and at mitosis the \verb{2k} copies partition \strong{at random} between the
two daughters (\verb{Binom(2k, 1/2)}), because ecDNA has no centromere. Cells are
then sampled in proportion to a fitness that rises with oncogene dosage.
}
\details{
Use it in place of \code{\link[=sim_replicate]{sim_replicate()}} when you want the copy level to \emph{follow
from} how long the tumour has been evolving and how strongly the amplicon is
selected, rather than being set by hand.
}
\section{Why division rate does not raise read support}{

Random segregation is copy-number-neutral in expectation --- replication
doubles, division halves, so \verb{E[copies in daughter] = copies in parent}. In a
growing tumour with both daughters retained the ecDNA pool grows in step with
the cell count, but so does the diploid background it is measured against, and
a sequencing library is loaded to a fixed depth rather than a fixed number of
cells. Read support is therefore a \strong{ratio} --- roughly
\verb{depth * (junction copies / genome copies) * purity} --- and dividing faster
leaves it unchanged. With \code{selection = 0} this function reproduces that: copy
number stays flat however many generations are run, while cell-to-cell
variance grows and a tail of cells loses the amplicon entirely. Only
\code{selection > 0} raises the population mean, at a rate of about
\verb{d log(copies)/d generation ~ selection}. Since bulk WGS sees only that mean,
the per-cell spread is invisible to it --- which is why \code{sim_replicate()}'s
deterministic growth is an adequate model for bulk data, and this function's
value is in deriving the level rather than asserting it.
}

\examples{
ec <- sim_episome(seed_locus("EGFR"), sample = "SEG01", copies = 20)

## neutral: twenty generations of division leave the level where it started
summary(sim_segregate(ec, generations = 20, selection = 0, seed = 1))$copies

## under selection it climbs
summary(sim_segregate(ec, generations = 20, selection = 0.1, seed = 1))$copies
}
\seealso{
\code{\link[=sim_replicate]{sim_replicate()}}, \code{\link[=sim_episome]{sim_episome()}}
}
