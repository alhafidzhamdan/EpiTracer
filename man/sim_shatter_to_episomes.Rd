% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-mechanisms.R
\name{sim_shatter_to_episomes}
\alias{sim_shatter_to_episomes}
\title{Simulate shattering into several new episomes}
\usage{
sim_shatter_to_episomes(
  x,
  n_breaks = 20L,
  n_circles = 3L,
  del_p = 0.1,
  inv_p = 0.5,
  require_oncogene = TRUE,
  keep_parent = FALSE,
  copies = 1,
  fitness = 1,
  homology = c("nhej", "mmej", "nahr"),
  seed = NULL
)
}
\arguments{
\item{x}{An \link{ecdna_sim}. The most abundant circle species is the one captured
in the micronucleus.}

\item{n_breaks}{Integer number of shattering breakpoints (default \code{20}).}

\item{n_circles}{Number of daughter circles the fragments religate into
(default \code{3}).}

\item{del_p}{Probability a fragment is lost outright during religation
(default \code{0.1}).}

\item{inv_p}{Probability a fragment is inverted (default \code{0.5}).}

\item{require_oncogene}{Logical; drop daughters that carry no preserved locus
(default \code{TRUE}). A circle without the oncogene confers no advantage and is
diluted out. Set \code{FALSE} to keep every daughter, modelling the moment before
selection has acted.}

\item{keep_parent}{Logical; does the intact parent circle survive alongside
the daughters (default \code{FALSE})? \code{FALSE} models the whole ecDNA population
passing through the micronucleus, so the daughters inherit and split its
copies. \code{TRUE} models a single molecule being shattered while the rest of the
population carries on, giving a parent that keeps its founder junction plus
daughters arising as minority species.}

\item{copies}{Copies per cell of each daughter when \code{keep_parent = TRUE}
(default \code{1}, a single molecule). Ignored when \code{keep_parent = FALSE}, where
the parent's copies are split evenly across the surviving daughters.}

\item{fitness}{Replication rate of the daughters relative to the parent
(default \code{1}). Daughters are shorter, so a value above \code{1} is the expected
direction; see \code{\link[=sim_internal_deletion]{sim_internal_deletion()}}.}

\item{homology}{Repair pathway sealing the new junctions (default \code{"nhej"}).}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}}.}
}
\value{
An \link{ecdna_sim} whose population is the surviving daughters (plus the
parent when \code{keep_parent = TRUE}). Its \code{history} gains
\code{"micronuclear_fission"}.
}
\description{
The ecDNA is encapsulated in a micronucleus and shattered, and the surviving
fragments religate into \strong{several separate circles} rather than one. Each
daughter is a new episome with its own circularisation junction, and the
population that results is a mixture of them.
}
\details{
This is the branch \code{\link[=sim_micronucleation]{sim_micronucleation()}} does not cover: that operator
religates the fragments back into a single circle, whereas here the fragments
partition, so one amplicon becomes a family of smaller ones.

Two consequences fall out that are worth simulating for:
\itemize{
\item \strong{The founder boundary duplication is usually destroyed.} It survives only
if some daughter happens to rejoin the two ends it joined, which is unlikely
once the circle has been cut in many places. An amplicon that has been
through fission therefore keeps a high, flat copy number over whatever its
surviving daughters retain, but has no duplication-orientation junction
spanning it --- so \code{\link[=call_simple_excision]{call_simple_excision()}} no longer recognises it, even
though it is still, genuinely, a set of episomes.
\item \strong{Copy number becomes patchy.} Sequence that ends up in a daughter that was
lost, or in one that failed selection, drops back to the host baseline, so
the flat amplicon of the parent breaks into blocks at different levels.
}
}
\examples{
ec <- sim_episome(seed_locus("EGFR"), sample = "FIS01", copies = 60)
fis <- sim_shatter_to_episomes(ec, n_breaks = 20, n_circles = 3, seed = 1)
fis                                   # several daughter species
## the founder boundary duplication is gone
fis$junctions[origin == "amplicon" & mechanism == "simple_excision"]
}
\references{
Shoshani, O. \emph{et al.} Chromothripsis drives the evolution of gene
amplification in cancer. \emph{Nature} \strong{591}, 137-141 (2021).
}
\seealso{
\code{\link[=sim_micronucleation]{sim_micronucleation()}}, \code{\link[=sim_episome]{sim_episome()}}, \code{\link[=call_simple_excision]{call_simple_excision()}}
}
