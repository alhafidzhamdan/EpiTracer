% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-emit.R
\name{sim_to_epitracer}
\alias{sim_to_epitracer}
\title{Render a simulated amplicon as EpiTracer caller inputs}
\usage{
sim_to_epitracer(x, noise = sim_noise(), min_cn_ratio = 3, seed = NULL)
}
\arguments{
\item{x}{An \link{ecdna_sim}, or a list of them (one per sample) to render as one
cohort.}

\item{noise}{A \code{\link[=sim_noise]{sim_noise()}} specification, or a numeric scale passed to it.}

\item{min_cn_ratio}{Amplification threshold used to define the reported
amplicon footprint, matching \code{\link[=detect_amplicon_seeds]{detect_amplicon_seeds()}} (default \code{3}).}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}}.}
}
\value{
A named list of four \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} --- \code{ecdna_gr},
\code{breakpoints_gr}, \code{cnv_gr}, \code{cancer_genes_gr} --- plus \code{truth}, a
\link[data.table:data.table]{data.table::data.table} of the ground-truth mechanism labels per sample.
The first four can be passed straight to any EpiTracer caller.
}
\description{
Turns an \link{ecdna_sim} into the four GRanges \code{\link[=call_simple_excision]{call_simple_excision()}} and the
mechanism callers take, applying a model of what a short-read pipeline would
actually report: finite purity and depth, copy-number segmentation error and
resolution, junction dropout and false positives, and repair-pathway
microhomology. With \code{noise = sim_noise(0)} the rendering is exact.
}
\examples{
ec <- sim_episome(seed_locus("EGFR"), sample = "SIM01", copies = 50)
inp <- sim_to_epitracer(ec, seed = 1)
res <- call_simple_excision(inp$ecdna_gr, inp$breakpoints_gr,
                            inp$cnv_gr, inp$cancer_genes_gr)
unique(res$episomal)
}
\seealso{
\code{\link[=sim_episome]{sim_episome()}}, \code{\link[=sim_to_plot_inputs]{sim_to_plot_inputs()}}, \code{\link[=sim_cohort]{sim_cohort()}}
}
