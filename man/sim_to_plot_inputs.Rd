% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-emit.R
\name{sim_to_plot_inputs}
\alias{sim_to_plot_inputs}
\title{Render a simulated amplicon as plotter inputs}
\usage{
sim_to_plot_inputs(x, noise = sim_noise(), seed = NULL)
}
\arguments{
\item{x}{An \link{ecdna_sim}, or a list of them.}

\item{noise}{A \code{\link[=sim_noise]{sim_noise()}} specification, or a numeric scale.}

\item{seed}{Optional integer passed to \code{\link[=set.seed]{set.seed()}}.}
}
\value{
A named list of three \code{data.frame}s --- \code{cnv_data}, \code{sv_data},
\code{wgd_data} --- matching \code{\link[=plot_sv_linear]{plot_sv_linear()}}'s arguments.
}
\description{
The same simulated amplicon in the data-frame format \code{\link[=plot_sv_linear]{plot_sv_linear()}} and
\code{\link[=plot_sv_reconstruction]{plot_sv_reconstruction()}} expect, so a simulated trajectory can be inspected
with the package's own viewers --- the quickest way to see whether a parameter
set reproduces the look of a patient amplicon.
}
\examples{
ec <- sim_evolve(sim_episome(seed_locus("EGFR"), sample = "SIM04"),
                 rounds = 2, n_breaks = 15, seed = 1)
pin <- sim_to_plot_inputs(ec, seed = 1)
p <- plot_sv_linear(sample = "SIM04", cnv_data = pin$cnv_data,
                    sv_data = pin$sv_data, wgd_data = pin$wgd_data,
                    chromosome = "chr7")
}
\seealso{
\code{\link[=sim_to_epitracer]{sim_to_epitracer()}}, \code{\link[=plot_sv_linear]{plot_sv_linear()}}, \code{\link[=plot_sv_reconstruction]{plot_sv_reconstruction()}}
}
