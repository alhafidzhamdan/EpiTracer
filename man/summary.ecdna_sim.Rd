% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-amplicon.R
\name{summary.ecdna_sim}
\alias{summary.ecdna_sim}
\title{Summarise a simulated amplicon}
\usage{
\method{summary}{ecdna_sim}(object, ...)
}
\arguments{
\item{object}{An \link{ecdna_sim} from \code{\link[=sim_episome]{sim_episome()}} and friends.}

\item{...}{Ignored.}
}
\value{
A one-row \link[data.table:data.table]{data.table::data.table} with the sample, structure,
fragment and junction counts, per-class junction counts, circle size,
copies per cell, peak copy number, number of chromosomes involved and the
mechanism history.
}
\description{
Ground-truth summary statistics of an \link{ecdna_sim} object -- the quantities the
mechanism callers try to recover, so a simulation can be checked against what
EpiTracer reports for it.
}
\examples{
ec <- sim_episome(seed_locus("EGFR"), sample = "SIM01")
summary(ec)
}
